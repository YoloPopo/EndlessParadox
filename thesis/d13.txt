\documentclass[12pt,a4paper,oneside]{report}

% --- Packages ---
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage{geometry}
\usepackage{setspace}
\usepackage{booktabs}
\usepackage{array}
\usepackage{float}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{xcolor}
\usepackage{enumitem}
\usepackage{microtype}
\usepackage{svg}
\usepackage[colorlinks=true, linkcolor=blue, citecolor=blue, urlcolor=blue]{hyperref}

% --- Unicode Character Binds ---
\DeclareUnicodeCharacter{2013}{--}
\DeclareUnicodeCharacter{2014}{---}
\DeclareUnicodeCharacter{00D7}{\ensurebtMath{\times}}
\DeclareUnicodeCharacter{223C}{\ensurebtMath{\sim}}
\DeclareUnicodeCharacter{2248}{\ensurebtMath{\approx}}
\DeclareUnicodeCharacter{2192}{\ensurebtMath{\rightarrow}}
\DeclareUnicodeCharacter{00B0}{\ensurebtMath{^\circ}}

% --- Page Setup (GOST 2.105-1995 compliant) ---
\geometry{left=30mm, right=15mm, top=20mm, bottom=20mm}
\onehalfspacing
\setlength{\parindent}{1.25cm}
\setlength{\parskip}{0pt}
\graphicspath{{figures/}}
\raggedbottom

% --- Page Numbering (GOST 7.32-2001) ---
\pagestyle{fancy}
\fancyhf{}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}

% --- Title Page ---
\title{
    \vspace{2cm}
    \textbf{\Large Adaptive Design of Embodied Robotic Systems for Real-Time Self-Modeling Toward Swarm Coordination} \\
    \vspace{1cm}
    \large Master's Thesis
}
\author{
    Muhammad Zeeshan Asghar \\
    Master's Program in Data Science \\
    HSE University
}
\date{\vspace{1cm} 2026}

\begin{document}

% --- Front Matter ---
\pagenumbering{gobble}
\maketitle
\clearpage

% --- Abstract ---
\chapter*{Abstract}
\addcontentsline{toc}{chapter}{Abstract}

Embodied robotic systems deployed in unstructured environments inevitably experience 
embodiment drift---the progressive divergence between a robot's internal kinematic model and its physical hardware caused by mechanical wear, calibration slip, thermal deformation, and unmodeled impacts \cite{bongard2006resilient, cully2015robots}.
When a robot's internal model no longer matches its physical embodiment, traditional Jacobian-based inverse kinematics controllers fail catastrophically: end-effectors diverge from targets, oscillations destabilize the system, and contact-rich tasks generate unsafe forces \cite{kwiatkowski2019task, chen2022full}.
Maintaining true autonomy requires self-modeling---the ability to infer one's own morphology directly from sensorimotor experience.
Contemporary visual self-modeling architectures based on Neural Radiance Fields (NeRF) and 3D Gaussian Splatting (3DGS) achieve high morphological expressiveness but suffer from a critical latency paradox \cite{hu2024teaching, hu2025learning}.
A diagnostic self-model is only valuable if it can provide state estimates fast enough to influence the feedback controller.
Modern robotic control loops commonly operate at 1~kHz, permitting a maximum latency of 1.0 millisecond per inference \cite{siciliano2009robotics}.
Existing neural rendering self-models operate at 5--37~FPS---latencies of 27--192 milliseconds---rendering them unusable for real-time control integration.
This represents a fundamental architectural mismatch: volumetric reconstruction is computationally incompatible with sub-millisecond control deadlines.

This thesis resolves this bottleneck by introducing NeuroKin, a lightweight fully convolutional direct sensorimotor decoder that completely bypasses explicit 3D volumetric reconstruction.
NeuroKin maps joint angles directly to visual silhouettes, achieving 21.88~dB PSNR, with a training-loop throughput of 7,406 samples/s (batch size 32) and a total training time of 33.4 seconds \cite{hu2024teaching}.
Training completes in 33.5 seconds on a Tesla T4 GPU.
An auxiliary multi-task variant, ResNeuroKin-D, recovers geometric depth while maintaining 2,500~FPS (0.400 ms latency).
Integration of NeuroKin-3D's stereo visual estimates into a Jacobian-based feedback loop enables dynamic error recovery after encoder-slip fault injection, demonstrating that sub-millisecond visual self-modeling can actively stabilize a damaged manipulator under proprioceptive drift.
Building on this individual diagnostic capability, we scale to collective fault tolerance via temporal stigmergy---a mechanism of indirect coordination wherein agents leave environmental traces that stimulate adaptive responses in subsequent agents.
In a 100-stage sequential swarm under progressive fault injection, the standard inverse-kinematics baseline collapses from 100\% pre-fault success to 13\% post-fault success.
Conversely, NeuroKin-enabled agents update a shared state (\texttt{factory\_state}) that tracks consecutive failures, autonomously trigger localized recovery modes, and preserve 88\% systemic success.

This thesis provides four primary contributions: (1) formal benchmarking of the latency paradox in neural rendering self-models, (2) the NeuroKin direct decoding architecture achieving high-throughput visual decoding, (3) visual closed-loop adaptation under encoder-slip faults, and (4) empirical validation that fast individual self-modeling enables decentralized stigmergic coordination---the computational prerequisite for physical swarm deployment.
\clearpage

% --- Acknowledgements ---
\chapter*{Acknowledgements}
\addcontentsline{toc}{chapter}{Acknowledgements}

This thesis represents the culmination of a deeply fulfilling academic journey, and it would not have been possible without the profound support of my mentors, colleagues, and family.
First and foremost, I gratefully acknowledge the guidance, patience, and critical feedback of my academic supervisor and the thesis defence committee at the Higher School of Economics (HSE) Master's Program in Data Science.
Your rigorous standards and technical insights have fundamentally shaped my approach to research.
Reflecting on my academic development, the foundational engineering principles I acquired during my Bachelor of Engineering in Mechatronics, Robotics, and Automation Engineering with a minor in Computing from the National University of Sciences and Technology (NUST) have been absolute prerequisites in grounding my deep learning models in real-world physical and kinematic boundaries.
I also extend my sincere appreciation to my co-founders and colleagues at LangMeet AI.
Serving as Technical Lead while managing real-time speech translation infrastructure concurrently with my thesis workload has been a formidable challenge, but it provided an invaluable sandbox for investigating computational latency profiles, hardware-level optimization, and production bottlenecks.

On a deeply personal level, I owe an immense debt of gratitude to my parents.
Your shared childhood observations, continuous encouragement, and steadfast support have been my ultimate cognitive anchor.
To my sister, whose empathy, kindness, and unyielding dedication to rescuing and adopting stray cats inspire me daily to notice harmony in chaotic systems.
To my brother, whose background studying classical violin provided a beautiful, resonant acoustic environment that acted as the backdrop to countless late-night debugging and writing sessions.
I am also deeply thankful to my aunt for her kind nature, warmth, and steady reassurance throughout my master's timeline.
Finally, I must acknowledge the quiet but essential companionship of my cat, Alec, who spent countless hours keeping watch beside my workstations while these neural networks were designed, trained, and evaluated.
As this Master's exploration draws to a close, I look forward to expanding this foundation.
The synthetic experiments and decentralized control frameworks validated herein represent an indispensable stepping stone.
Translating these direct sensorimotor networks from controlled physical constraints into adaptive physical deployments on multi-robot platforms will form the explicit technical objective of my upcoming Ph.D. program.

Supplementary materials, dataset generation code, and the implementation of the NeuroKin project are available in the public repository: \url{https://github.com/YoloPopo/neurokin}.
The author confirms that AI tools were used only to assist with grammar, vocabulary, and to enhance narrative flow;
all technical content, experimental design, analyses, and conclusions remain the author's original work.
\clearpage

\tableofcontents
\clearpage
\listoffigures
\addcontentsline{toc}{chapter}{List of Figures}
\clearpage
\listoftables
\addcontentsline{toc}{chapter}{List of Tables}
\clearpage

% --- Main Matter ---
\pagenumbering{arabic}

% ==========================================
% CHAPTER 1: INTRODUCTION
% ==========================================
\chapter{Introduction}
\label{chap:intro}

\section{The Challenge of Embodiment Drift}
\label{sec:challenge_embodiment_drift}

Autonomous robots deployed in remote, unstructured environments---planetary rovers, disaster-response machines, deep-sea manipulators, and industrial inspection drones---must operate for extended periods without human intervention.
These systems cannot rely on frequent manual recalibration, hardware replacement, or human-in-the-loop supervision.
They must be self-sufficient, adaptive, and resilient in the face of inevitable physical degradation \cite{bongard2006resilient, cully2015robots}.
A fundamental requirement for such autonomy is maintaining an accurate internal representation of one's own morphology, kinematics, and dynamics.
Traditional robotic systems rely on predefined kinematic models---typically specified in Unified Robot Description Format (URDF) files---that encode link lengths, joint limits, inertial properties, and collision geometries.
These models enable classical control algorithms, such as Jacobian-based inverse kinematics and operational space control, to map intended high-level actions into precise joint torques or positional trajectories \cite{siciliano2009robotics}.
Under ideal conditions, with a perfectly calibrated robot operating in a controlled environment, these methods achieve millimeter-level precision.

However, the assumption that a physical robot remains mathematically identical to its idealized digital model is fundamentally flawed under continuous operational deployment.
Physical embodiment subjects robots to the relentless realities of thermodynamics, material fatigue, and mechanical friction.
Over extended operational cycles---spanning hours, days, or months of continuous operation---the strict mathematical guarantees of forward and inverse kinematics inevitably decouple from physical reality.
This persistent, accumulating divergence between the internal computational representation of the robot and its actual physical state is formally defined in this thesis as \textit{embodiment drift} \cite{bongard2006resilient, kwiatkowski2019task}.
The term captures the essential phenomenon: the robot's sense of its own body drifts away from truth as hardware degrades.

\subsection{Manifestations of Embodiment Drift}
\label{subsec:manifestations_embodiment_drift}

Embodiment drift manifests across a broad spectrum of mechanical and sensory degradations.
At the micro-scale, geared transmissions develop unmodeled backlash as teeth wear;
actuator drive belts lose optimal tension through creep and fatigue;
thermal expansion induced by sustained high-torque operations alters the effective physical length of metallic linkages by micrometers---enough to matter in precision tasks.
At the sensory layer, proprioceptive joint encoders may imperceptibly slip from their calibrated zero-points due to vibration or electrical noise;
exteroceptive sensors, such as chassis-mounted RGB cameras or depth sensors, experience extrinsic calibration shifts from ambient thermal cycling or minor physical impacts \cite{rezvani2024robust}.
In more severe, sudden operational scenarios, robots may suffer macroscopic structural damage: an unexpected collision may plastically deform a physical link by several degrees;
an actuator may seize entirely due to electrical failure or debris ingress;
an asymmetrical payload shift---such as a grasped object suddenly changing weight distribution---may fundamentally alter the system's center of mass and dynamic response.

\subsection{Consequences for Control}
\label{subsec:consequences_control}

When significant embodiment drift occurs, the consequences for closed-loop feedback control are catastrophic.
A controller operating under the assumption of a nominal kinematic tree will compute pseudo-inverse Jacobian updates that are mathematically sound relative to the model but physically erroneous relative to reality \cite{siciliano2009robotics}.
The system issues corrective motor commands that inadvertently drive the end-effector away from the target, induce highly unstable resonant oscillations, or, in contact-rich manipulation tasks, exert unsafe, unbounded forces upon the environment.
In legged locomotion, embodiment drift leads to gait asymmetry, reduced stability margins, and eventual falls.
In humanoid robots operating near humans, the safety implications are even more severe \cite{kwiatkowski2019task}.
To maintain true autonomy and operational safety in the presence of embodiment drift, a robotic system must possess the capacity for \textit{self-modeling}---the ability to autonomously infer, update, and deploy an internal representation of its own morphology derived strictly from its own sensorimotor experience, entirely bypassing the need for external, human-in-the-loop manual recalibration \cite{chen2022full, kwiatkowski2019task}.
Figure~\ref{fig:self_modeling_pipeline_pdf} summarizes the closed-loop self-modeling pipeline that motivates the rest of this thesis.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{self_modeling_pipeline.pdf}
    \caption{Block diagram tracing a five-stage operational architecture for Training / adaptation Sensing RGB, depth, IMU Encoding features and state Self-Model morphology, kinematics, and dynamics Planning control and recovery Actuation motion and manipulation closed-loop residuals.}
    \label{fig:self_modeling_pipeline_pdf}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{timeline_self_modeling.pdf}
    \caption{Horizontal developmental timeline marking benchmark historical milestones in robotic self-modeling research from early 2006 symbolic explorations to contemporary real-time neural rendering architectures.}
    \label{fig:timeline_self_modeling_pdf}
\end{figure}

\section{The Evolution of Self-Modeling Paradigms}
\label{sec:evolution_self_modeling}

The formidable challenge of autonomous self-modeling has driven several distinct research paradigms over the past two decades. Each era brought unique insights and faced specific limitations that motivate the approach taken in this thesis \cite{hu2024teaching, hu2025learning}. Figure~\ref{fig:timeline_self_modeling_pdf} provides a visual summary of this progression. Early approaches relied heavily on symbolic inference and active physical exploration, utilizing continuous cycles of hypothesis generation to deduce morphological changes. Subsequent paradigms shifted toward behavioral adaptation, bypassing damage diagnosis to search pre-computed behavioral repertoires for compensatory actions. The advent of deep learning introduced task-agnostic learned dynamics models, while parallel threads explored proprioceptive morphology identification to restore embodiment using internal signals alone.

Most recently, the field has entered the visual era, leveraging neural rendering to learn self-models directly from camera observations. Architectures based on implicit coordinate networks like the Free-Form Kinematic Self-Model (FFKSM) and explicit primitives like Kinematic 3D Gaussian Splatting (K-3DGS) achieve high morphological expressiveness. However, as this thesis establishes, the immense representational power of these neural rendering architectures introduces a severe computational bottleneck. A detailed chronological analysis of these paradigms and their respective mathematical limitations is provided in Chapter 2.

\section{The Latency Paradox in Neural Rendering}
\label{sec:latency_paradox}

A diagnostic self-model is operationally valuable only if it can provide state estimates fast enough to influence the stabilizing behavior of the feedback controller \cite{hu2024teaching, hu2025learning}. If the model requires hundreds of milliseconds to generate a prediction, the information arrives too late---the robot has already executed several control cycles based on stale assumptions.

\subsection{Temporal Constraints of Robotic Control}
\label{subsec:temporal_constraints}

Modern robotic control systems operate at tightly constrained frequencies. Force control, impedance control, and dynamic balancing typically run at 1 kHz, requiring the entire perception-planning-actuation pipeline to execute within 1.0 millisecond \cite{siciliano2009robotics}. Table~\ref{tab:control_loop_latency} shows the latency requirements for different control regimes.

\begin{table}[H]
\centering
\caption{Latency requirements for robotic control loops at different frequencies.}
\label{tab:control_loop_latency}
\begin{tabular}{lcc}
\toprule
\textbf{Control Type} & \textbf{Frequency} & \textbf{Max Latency (ms)} \\
\midrule
Force/Impedance Control & 1 kHz & 1.0 \\
Compliant Manipulation & 500 Hz & 2.0 \\
Visual Servoing & 30--100 Hz & 10.0--33.3 \\
Offline Diagnostics & <10 Hz & >100.0 \\
\bottomrule
\end{tabular}
\end{table}

Within the 1.0 ms window, the entire software stack must read physical sensor buffers, execute state estimation, compute target Cartesian error vectors, resolve complex inverse kinematic equations, enforce physical joint limits, and issue subsequent motor torque commands. Consequently, any self-modeling diagnostic component integrated into this inner control loop possesses an absolute maximum inference budget of a fraction of a millisecond.

\subsection{The Computational Bottleneck}
\label{subsec:computational_bottleneck}

Current state-of-the-art neural rendering architectures fundamentally violate this latency budget. Implicit NeRF-based models achieve frame rates of approximately 5 FPS, corresponding to 191.6 ms latency \cite{hu2024teaching}. Explicit 3DGS-based architectures reach approximately 37 FPS, corresponding to 27.0 ms latency \cite{hu2025learning}. Both exceed the 1.0 ms control budget by factors of 27 to 190---rendering them entirely unsuitable for real-time control integration.

The root cause is architectural. Both approaches maintain an explicit or implicit 3D volumetric representation. NeRF requires evaluating a deep MLP hundreds of thousands of times per image via ray-marching---a fundamentally serial process \cite{mildenhall2020nerf}. Even with acceleration structures like Instant-NGP's hash encoding \cite{muller2022instant} or Mip-NeRF's scale-aware anti-aliasing \cite{barron2021mip}, the latency remains too high for tight control loops required in robotic damage recovery. Adapting NeRFs to articulated bodies via D-NeRF approaches \cite{pumarola2021d} adds time-deformation networks, further compounding computational cost. 3DGS requires computing forward kinematics for thousands of Gaussian primitives, projecting them into 2D screen space, sorting them by depth, and alpha-compositing \cite{kerbl20233d}---a heavily sequential pipeline despite GPU acceleration. While 3DGS achieves substantial speedup over NeRF, this speed is achieved by retreating to isotropic constraints that yield geometric artifacts, dropping fidelity \cite{hu2025learning}. These operations are not merely engineering inefficiencies; they are structural consequences of demanding a full 3D intermediate representation.

\subsection{The Plasticity-Stability Dilemma in Self-Modeling}
\label{subsec:plasticity_stability}

Beyond latency, self-modeling faces the plasticity-stability dilemma, first articulated by Grossberg \cite{grossberg1987competitive}: the system must be plastic enough to learn new configurations (damage) yet stable enough to retain knowledge of undamaged components. In biological brains, this balance is achieved through synaptic pruning---the physical restructuring of the connectome wherein weak neuronal connections are eliminated while frequently used connections are strengthened. Hebbian learning ensures that functional modules are physically distinct.

In standard, monolithic deep neural networks, sequentially learning new spatial geometries leads inevitably to catastrophic forgetting \cite{mccloskey1989catastrophic, french1999catastrophic}. If an implicit NeRF, an explicit 3DGS point cloud, or a direct convolutional decoder is naively fine-tuned exclusively on a stream of recent visual observations depicting a damaged joint, the globally shared gradient updates will rapidly destroy the network's ability to accurately render the undamaged base of the robot. The network overfits to the new, damaged data distribution at the catastrophic expense of prior structural knowledge \cite{kirkpatrick2017overcoming}. To combat this sequential learning degradation, the literature has developed extensive mitigation frameworks. Regularization-based methodologies, such as Elastic Weight Consolidation (EWC) \cite{kirkpatrick2017overcoming}, calculate the Fisher information matrix to identify and mathematically protect specific neural weights crucial for maintaining prior geometric knowledge. Architectural isolation methods rely on dynamic routing protocols, mixture-of-experts ensembles \cite{mocanu2018scalable}, or isolating continuous learning within highly sparse, trainable subnetworks identified via the Lottery Ticket Hypothesis \cite{frankle2018lottery}. However, these methods remain computationally expensive for real-time adaptation on edge hardware, and they treat the network as a monolith with varying ``stiffness'' rather than enabling true structural modularity.

\section{Digital Twins and the Simulation-to-Reality Gap}
\label{sec:digital_twins}

The endeavor to maintain an accurate, real-time internal self-model is conceptually identical to the broader industrial engineering goal of generating and sustaining a robotic Digital Twin \cite{jiang2024digitaltwin, mari2024digitaltwin}. In contemporary academic literature, a digital twin is defined not merely as a static 3D visual replica or a passive CAD model, but as a bi-directional, dynamically synchronized computational surrogate of a physical asset. The idealized robotic digital twin operates in continuous tandem with the physical hardware. It receives high-frequency sensor telemetry (joint angles, motor torques, camera feeds) from the physical robot in the field. Crucially, it dynamically updates its internal state matrices---its collision geometry, mass distribution, and friction coefficients---to perfectly mirror the physical degradation and embodiment drift experienced by the hardware. This dynamic synchronization allows the twin to serve as a perfectly safe, highly accelerated sandbox for forward-simulating potential control policies before executing them in physical reality \cite{sun2026digitaltwin, murugesan2024digitaltwin}. Figure~\ref{fig:digital_twin_loop} illustrates the continuous synchronization loop between a physical robot and its digital twin.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{sim2real_digital_twin.pdf}
    \caption{System workflow chart mapping continuous data collection loops from physical hardware to calibrate a simulation environment, detailing online deployment execution and residual error matching pathways. Measure, model, deploy, correct mismatch on Real Robot hardware and sensors through Data Capture, Training / calibration, and Residual correction loops.}
    \label{fig:digital_twin_loop}
\end{figure}

However, constructing visual digital twins specifically intended for autonomous, closed-loop robotic control introduces stringent latency constraints that differ significantly from those found in static industrial monitoring. An active robotic digital twin must be instantaneously queryable. If a physical manipulator operating in a highly cluttered environment sustains an unexpected impact, the digital twin must update its geometric occupancy map immediately. If the twin's internal update latency exceeds the temporal dynamics of the physical interaction, the robot will execute evasive commands based on stale, pre-impact geometry, precipitating further catastrophic collisions.

Furthermore, the practical deployment of neural self-models and dynamic digital twins is universally haunted by the Simulation-to-Reality (Sim-to-Real) gap \cite{tan2018simtoreal, aljalbout2026realitygap}. The majority of high-capacity neural representations are trained within pristine simulation environments, such as MuJoCo, Isaac Gym, or, as utilized in this thesis, PyBullet. While computationally efficient, these physics simulators rely on idealized mathematical approximations. They frequently fail to accurately capture complex Coulomb friction models, non-linear actuator stiction, subtle link compliance under heavy payloads, and the incredibly complex photometric realities of physical environments, including ambient lighting fluctuations, harsh specular reflections on metallic links, and pervasive sensor noise.

When a neural self-model trained exclusively in simulation is deployed onto physical hardware, it frequently collapses. The minor photometric discrepancies between the simulated training data and the physical camera feed severely corrupt the network's latent space, resulting in completely erroneous morphological predictions \cite{rezvani2024robust}. Recent literature attempts to bridge this critical Sim-to-Real gap through massive Domain Randomization, where researchers randomly perturb lighting vectors, background textures, camera positions, and physical mass properties during simulation training \cite{tan2018simtoreal}. Advanced methodologies also explore real-time Residual Dynamics Learning, wherein the neural network is trained to predict only the small, non-linear residual discrepancy between the idealized physics simulator and the actual physical observation \cite{qiu2024swim2real, lim2025splat2real}.

Within the rigorous scope of this thesis, all experiments are deliberately bounded within the synthetic PyBullet simulation environment. This is not an academic oversight but a highly controlled, necessary methodological choice designed specifically to isolate architectural latency. By operating within the controlled vacuum of a simulator, this thesis effectively isolates the theoretical mathematical efficiency of the proposed NeuroKin architecture, demonstrating that the latency bottleneck can be reduced before introducing the complexities of physical noise.

\section{Problem Statement and Core Hypothesis}
\label{sec:problem_hypothesis_intro}

The core scientific problem addressed by this thesis is the resolution of the latency paradox in visual robotic self-modeling, and the subsequent leveraging of this high-speed diagnostic capability to establish the computational mechanisms for multi-agent swarm coordination under compounding mechanical failures.

\subsection{Formal Mathematical Problem Statement}
\label{subsec:formal_mathematical_problem_statement_intro}

We formally define the true physical morphology of the robot at time $t$ as the configuration manifold $\mathcal{M}(t)$. The robotic system maintains an internal computational kinematic representation $\hat{\mathcal{M}}(t)$. Embodiment drift is defined as the non-stationary, cumulative divergence between the physical reality and the digital expectation:
\begin{equation}
D(t) = \|\mathcal{M}(t) - \hat{\mathcal{M}}(t)\|
\end{equation}

When an unmodeled kinematic fault $\Delta \mathbf{q}_{\text{fault}}$ occurs, the proprioceptive encoder estimate $\mathbf{q}_{\text{enc}}$ deviates from the true physical state $\mathbf{q}_{\text{true}}$ such that $\mathbf{q}_{\text{true}} = \mathbf{q}_{\text{enc}} + \Delta \mathbf{q}_{\text{fault}}$. This breaks traditional closed-loop control. 

Let the sensory observation space be $\mathcal{O}$ (in this case, binary visual silhouettes) and the joint configuration space be $\mathcal{Q}$. The objective of the visual self-model is to learn a direct sensorimotor mapping $f_\theta: \mathcal{Q} \rightarrow \mathcal{O}$ that minimizes the reconstruction loss $\mathcal{L}(f_\theta(\mathbf{q}), \mathbf{o})$ to recover the true Cartesian end-effector position $\mathbf{x}_{\text{true}}$. Crucially, this mapping is strictly bound by control-loop temporal dynamics. If the control system operates at frequency $f_{ctrl}$, dictating a strict maximum latency constraint $T_{max} = 1/f_{ctrl}$, and $t_{\text{infer}}$ is the inference time of the self-model, the system is physically deployable for closed-loop stabilization if and only if:
\begin{equation}
t_{\text{infer}} \le T_{max}
\end{equation}
For a standard $1$~kHz feedback loop, this necessitates $t_{\text{infer}} \le 1.0$~ms.

\subsection{The Central Hypothesis}
\label{subsec:central_hypothesis_intro}

This thesis investigates a central hypothesis: \textit{explicit 3D volumetric reconstruction is not a prerequisite for control-oriented self-modeling}. We propose a paradigm shift toward \textit{direct sensorimotor decoding}: a lightweight fully convolutional network that maps joint angles directly to visual silhouettes and spatial coordinates, completely bypassing the intermediate 3D reconstruction step. We hypothesize that such an architecture can achieve sub-millisecond inference latency---compatible with 1 kHz control budgets---while maintaining reconstruction fidelity comparable to or exceeding volumetric baselines, precisely because dense pixel-wise supervision is more efficient than sparse ray sampling.

\subsection{Empirical Scope, Constraints, and Hardware Realities}
\label{subsec:empirical_scope_intro}

The empirical scope of this research is deliberately bounded within a highly controlled, synthetic PyBullet simulation environment. This is a deliberate necessity. Implementation on actual physical hardware is not possible within the scope of this Master's thesis due to severe, practical resource limitations. The rigorous validation of dynamic fault adaptation requires dedicated, damageable physical manipulators where structural faults can be safely and repeatedly injected. Furthermore, acquiring ground-truth millimetric spatial coordinate data to validate the visual network's predictions requires expensive, high-speed motion-capture rigs (e.g., Vicon systems). Given the strict temporal and hardware constraints of the degree program, physical deployment is unattainable at this stage. Consequently, the transition from simulated architectural validation to physical hardware deployment---including addressing the complex photometric noise of the Sim-to-Real gap---is formally deferred to the author's upcoming Ph.D. research.

\subsection{The Temporal Stigmergy Framework for Swarm Coordination}
\label{subsec:temporal_stigmergy_framework_intro}

This thesis evaluates multi-agent coordination via \textit{temporal stigmergy}. Stigmergy is a biological mechanism of indirect coordination observed in social insects such as termites and ants. Individual agents do not communicate directly; instead, they alter the environment in ways that stimulate adaptive responses in subsequent agents. In this thesis, the shared ``environment'' is algorithmically abstracted into a persistent digital memory ledger---the \texttt{factory\_state} variable. This state-dependent, history-aware behavioral adaptation is the absolute core logic of decentralized swarm coordination. Proving that sub-millisecond individual self-diagnosis enables such temporal coordination validates the computational prerequisite for eventual physical swarms.

\section{Research Questions}
\label{sec:research_questions_intro}

Four research questions guide this investigation, progressing from architectural efficiency through control integration and fault tolerance to collective adaptation.

\subsection{RQ1: Architectural Efficiency}
\label{subsec:RQ1_intro}

To what extent can the computational latency of visual robot self-modeling be reduced by replacing 3D implicit and explicit volumetric rendering paradigms with 2D direct sensorimotor decoding, and what is the resulting impact on morphological reconstruction fidelity measured in PSNR?

\subsection{RQ2: Control Integration}
\label{subsec:RQ2_intro}

Can a visual self-model operating purely on direct convolutional decoding provide spatial coordinate estimates with sufficient accuracy and sub-millisecond latency to actively stabilize a Jacobian-based inverse kinematics control loop?

\subsection{RQ3: Fault Tolerance}
\label{subsec:RQ3_intro}

How does the integration of sub-millisecond visual self-estimates alter end-effector trajectory and error convergence compared to a purely proprioceptive baseline under induced mechanical faults such as severe encoder slip and structural damage?

\subsection{RQ4: Collective Adaptation}
\label{subsec:RQ4_intro}

Can the individual computational efficiency of a sub-millisecond self-model enable decentralized, state-dependent adaptation across a sequential pipeline of agents, thereby validating the temporal stigmergy mechanics required for future multi-agent swarm coordination?

\section{Methodological Contributions}
\label{sec:methodological_contributions_intro}

This thesis makes four primary contributions:

\subsection{Contribution 1: Formal Benchmarking of the Latency Paradox}

The first contribution is the formal benchmarking of the latency paradox in neural rendering, exposing their control incompatibility relative to the 1 ms control budget.

\subsection{Contribution 2: The NeuroKin Direct Sensorimotor Decoder}

The second contribution is the NeuroKin architecture. NeuroKin is a lightweight, fully convolutional direct sensorimotor decoder that completely bypasses volumetric reconstruction, mapping joint angles directly to visual silhouettes. NeuroKin achieves 21.88 dB PSNR at 7,400 FPS (0.135 ms latency)---a 1,418$\times$ speedup over implicit NeRF baselines.

\subsection{Contribution 3: Visual Closed-Loop Damage Adaptation}

The third contribution lies in visual closed-loop control and dynamic damage adaptation. This research successfully synthesizes NeuroKin's sub-millisecond spatial estimates with a Jacobian-based inverse-kinematics feedback loop, allowing the controller to re-stabilize the end-effector trajectory post-fault.

\subsection{Contribution 4: Temporal Stigmergy for Sequential Swarm Coordination}

The fourth contribution is the validation of stigmergic swarm coordination. The NeuroKin-enabled 100-stage sequential swarm maintains 88\% success post-fault compared to the 13\% success rate of the baseline.

\section{Thesis Organization}
\label{sec:thesis_organization_intro}

The remainder of this thesis is structured to systematically address the research questions. Chapter 2 provides an exhaustive critical synthesis of relevant literature. Chapter 3 details the empirical simulation methodology and latency bottleneck, outlining the PyBullet data generation pipeline. Chapter 4 formally introduces the core technical innovation---the NeuroKin architecture. Chapter 5 moves from static rendering metrics to dynamic control, outlining the integration of NeuroKin into an inverse-kinematics loop. Chapter 6 scales validation to the collective level, unpacking the 100-stage operational swarm chain and the mechanics of the inter-agent communication ledger. Chapter 7 evaluates the overarching hypothesis, Pareto trade-offs, and threats to validity. Finally, Chapter 8 synthesizes the research findings, directly answers the formal research questions, and maps a concrete technological roadmap for transitioning these validated sequential capabilities into physically embodied, spatially concurrent swarm deployments.
\clearpage

% ==========================================
% CHAPTER 2: LITERATURE SYNTHESIS
% ==========================================
\chapter{Literature Synthesis}
\label{chap:literature_synthesis}

\section{Evolution of Self-Modeling}

The capacity for autonomous adaptation to physical damage is the defining characteristic of biological resilience, yet it remains an elusive goal for robotic systems. This section treats the mathematical and conceptual structures underpinning robotic self-modeling across three distinct historical blocks---symbolic exploration, behavioral detour maps, and modern deep neural rendering methods---before isolating the foundational speed ceilings and system stability challenges motivating our work. Figure~\ref{fig:survey_taxonomy_pdf} maps this field to structure our critical literature synthesis.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{survey_taxonomy.pdf}
    \caption{Historical taxonomy of robot self-modeling paradigms: chronological evolution from symbolic approaches (2006) through behavioral learning, learned representations, visual estimation, to deployable systems (2026).}
    \label{fig:survey_taxonomy_pdf}
\end{figure}

\subsection{The Symbolic Era: Estimation-Exploration}
\label{subsec:the_symbolic_era_estimation_exploration}

The earliest formalized paradigm relied heavily on symbolic and kinematic inference. The seminal work of Bongard, Zykov, and Lipson established the first complete closed-loop self-modeling system on actual physical robots \cite{bongard2006resilient}. Their approach viewed self-modeling as a continuous cycle of hypothesis generation and active physical exploration. The robot maintained a population of competing symbolic models representing various candidate topologies---different possible configurations of its own body. To discriminate among these hypotheses, the robot systematically synthesized exploratory motor commands mathematically designed to maximize the sensory disagreement among the candidate models. Upon executing these actions, the robot collected sensor data and systematically pruned models that failed to predict observed outcomes.

After approximately 16 modeling-testing cycles spanning several minutes of real-world experimentation, the robot converged on an accurate damaged model and synthesized a compensatory gait through reinforcement learning on the internal simulation. The core innovation was an active learning loop: the fitness of a test action was determined by its ability to cause disagreement among the best candidate models, thereby maximally reducing uncertainty. While conceptually elegant, this approach suffers from the symbolic bottleneck: the robot can only discover models constructible from predefined physics engine primitives (cylinders, hinge joints). It cannot represent non-rigid deformations, soft-body dynamics, or arbitrary visual damage---limitations that become critical in unstructured environments where failure modes are unpredictable \cite{kwiatkowski2019task}. As noted by Kwiatkowski and Lipson, the search space of symbolic physics engines is discrete and non-differentiable, making optimization slow and prone to local optima \cite{kwiatkowski2019task}. Furthermore, the approach relied heavily on simple proprioceptive sensors providing limited information compared to high-bandwidth vision.

\subsection{The Behavioral Era: Avoidance Over Adaptation}
\label{subsec:the_behavioral_era_avoidance_over_adaptation}

Recognizing the computational intensity of explicit physical modeling, Cully and colleagues introduced Intelligent Trial and Error (IT\&E), which bypassed damage diagnosis entirely \cite{cully2015robots}. Their key insight was to focus on finding what still works rather than diagnosing what went wrong. Using the MAP-Elites quality-diversity algorithm \cite{mouret2015illuminating}, they pre-computed a behavioral repertoire of approximately 13,000 distinct gaits in simulation---a process requiring 40 million simulations over two weeks of computation. This repertoire is a high-dimensional grid where each cell corresponds to a specific behavioral descriptor (e.g., duty cycle versus leg offset) and stores the highest-performing controller found for that descriptor.

When damaged in the field, the robot treated this map as a Bayesian prior, using Gaussian Process regression to search for a behavior that performs well in the real world. By testing a few dozen distinct behaviors, the Gaussian Process updated its posterior mean and variance, rapidly identifying a compensatory gait. Cully and colleagues demonstrated recovery in less than two minutes of real-world testing across various damage scenarios, including removal of entire legs \cite{cully2015robots}. However, IT\&E represents a strategy of avoidance, not adaptation. The robot does not repair its internal model---it discards the parts of the behavioral space that no longer function. While effective for redundant locomotion tasks where multiple gaits can achieve similar forward velocity, this approach fails for precision manipulation tasks that require exact geometric awareness \cite{kwiatkowski2019task}. If a robot needs to reach through a narrow aperture, it cannot simply ``try a different gait''---it must understand the precise Cartesian geometry of its damaged arm. Furthermore, as robotic complexity increases, the curse of dimensionality makes pre-computing dense behavioral maps prohibitively expensive: a 6-DOF arm with even modest discretization would require billions of map entries, far exceeding computational budgets.

\subsection{Task-Agnostic Learned Dynamics Models}
\label{subsec:task_agnostic_learned_dynamics_models}

The advent of deep learning enabled a third paradigm: learning dynamics models that encode the mapping from motor commands to sensory consequences. Kwiatkowski and Lipson proposed task-agnostic self-modeling, training deep forward models to predict the future state of the robot given the current state and action \cite{kwiatkowski2019task}. This represented a pivotal shift from symbolic physics to learnable physics. The same approach was demonstrated to work as a complete reinforcement learning environment to learn zero-shot tasks across morphologies in a follow-up study \cite{kwiatkowski2019zeroshot}. While theoretically powerful, these data-driven models face a fundamental limitation: systematic bias accumulates progressively across extended planning horizons \cite{kwiatkowski2019task}. Simulated trajectories diverge increasingly from actual robot behavior, rendering the approach impractical for tasks requiring accurate prediction beyond very short time windows. This horizon-dependent degradation motivates approaches that prioritize shorter planning windows or use learned corrections rather than pure forward simulation.

\subsection{Proprioceptive Morphology Identification}
\label{subsec:proprioceptive_morphology_identification}

A parallel thread of research focuses on proprioceptive identification---restoring embodiment using internal signals alone, without the occlusions and lighting failures that often cause vision-only pipelines to break \cite{hu2024reconfigurable}. This offers significant deployment advantages: the approach remains robust across diverse lighting conditions and operates without calibrated cameras. However, eliminating external visual information imposes real functional costs: expressiveness is substantially reduced compared to vision-based methods \cite{farghdani2025robust}. This is not merely a theoretical gap but a genuine operating constraint that limits the complexity of tasks these systems can support compared to their multi-modal counterparts.

Meta-self-modeling restores morphology parameters between motion signature reconfigurations \cite{hu2024reconfigurable}, and IMU-centric methods show hardware-realized damage sensing with minimal sensing overhead on legged robots \cite{farghdani2025robust}. Analytical modular pipelines remain strong baselines in the case of known assembly constraints, offering interpretable, stable, and automatically generated kinematic and dynamic models \cite{dogra2022unified}. The frontier is intervention, not just identification: controlled breakage of proprioception-driven morphology demonstrates that active structural adaptation is supported by internal sensing, and that morphology itself is a form of adaptive feedback control \cite{xu2026antenna, yu2026kinematicself}.

\subsection{The Visual Era: Deep Self-Models and Neural Rendering}
\label{subsec:the_visual_era_deep_self_models_and_neural_rendering}

The current state-of-the-art leverages neural rendering to learn visual self-models directly from camera observations. Chen and colleagues introduced occupancy query learning as a foundational approach \cite{chen2022full}. Signed Distance Functions (SDFs) conditioned on joint angles enable differentiable collision checking directly from RGB-D sensor streams, supporting both motion planning and recovery from damage. Their multi-view hardware system achieved approximately one percent workspace accuracy and demonstrated support for planning and post-damage recovery. However, practical deployment introduced significant challenges: careful camera placement became critical for observability, precise calibration was required for geometric accuracy, and performance degraded substantially under occlusion, shadows, blur, and noise \cite{chen2022full}.

Hu, Lin, and Lipson reduced the camera system to a single RGB sensor while learning the Free-Form Kinematic Self-Model (FFKSM) from a single viewpoint \cite{hu2024teaching}. The reparameterization of camera frames in terms of the first two joints facilitated the learning process. This approach supports inverse kinematics, path planning, and post-damage recovery. However, volumetric rendering via NeRF remained computationally expensive, and single-view training introduced persistent vulnerabilities: occlusion sensitivity and perspective-dependent predictions. More recently, Kinematic 3D Gaussian Splatting (K-3DGS) has emerged as an explicit alternative to implicit NeRFs \cite{hu2025learning}. 3DGS replaces ray marching with differentiable rasterization of explicit anisotropic Gaussians, enabling high frame rate rendering and fast optimization. By attaching explicit Gaussian primitives to a differentiable kinematic chain and leveraging rasterization-based rendering rather than ray-marching, K-3DGS promises faster inference. Each Gaussian primitive is defined by a 3D mean position, covariance matrix, opacity, and color, allowing the model to render the robot in any arbitrary pose through standard graphics pipeline operations. Yet, as this thesis establishes, the immense representational power of these neural rendering architectures has introduced a severe computational bottleneck that completely obstructs physical deployment in real-time control loops.

\section{Neural Rendering Foundations: The Speed-Quality Tradeoff}

The computational bottleneck of visual self-modeling stems fundamentally from the choice of 3D representation. This section examines the mathematical foundations of Neural Radiance Fields (NeRF) and 3D Gaussian Splatting (3DGS), the two dominant paradigms for neural scene representation, while also surveying the broader landscape of articulated object reconstruction and digital twin generation.

\subsection{Neural Radiance Fields: Implicit Scene Representation}
\label{subsec:neural_radiance_fields_implicit_scene_representation}

Neural Radiance Fields (NeRFs), introduced by Mildenhall and colleagues, revolutionized computer vision by representing scenes as continuous functions mapping 5D coordinates to color and volume density \cite{mildenhall2020nerf}. The function $F_\theta: (\mathbf{x}, \mathbf{d}) \rightarrow (\mathbf{c}, \sigma)$ is parameterized by a multi-layer perceptron (MLP), where $\mathbf{x} \in \mathbb{R}^3$ is a spatial position, $\mathbf{d} \in \mathbb{R}^3$ is a viewing direction, $\mathbf{c} \in \mathbb{R}^3$ is the emitted color, and $\sigma \in \mathbb{R}^+$ is the volume density. To render a pixel, NeRF approximates the volume rendering integral via quadrature along a ray $\mathbf{r}(t) = \mathbf{o} + t\mathbf{d}$, where $\mathbf{o}$ is the camera origin and $\mathbf{d}$ is the ray direction. The expected color $C(\mathbf{r})$ is:
\begin{equation}
C(\mathbf{r}) = \int_{t_n}^{t_f} T(t) \sigma(\mathbf{r}(t)) \mathbf{c}(\mathbf{r}(t), \mathbf{d}) \, dt
\end{equation}

where $T(t) = \exp\left(-\int_{t_n}^{t} \sigma(\mathbf{r}(s)) \, ds\right)$ is the accumulated transmittance. In practice, this continuous integral is approximated via numerical quadrature using stratified sampling. For a standard $100 \times 100$ image with $N = 64$ samples per ray, this formulation requires $100 \times 100 \times 64 = 640,000$ network evaluations per frame \cite{mildenhall2020nerf}. At 5.22~FPS, this amounts to 3.2 million MLP evaluations per second---a computational burden that our subsequent implementations directly address. To capture high-frequency geometric details, NeRF employs positional encoding, mapping input coordinates into a higher-dimensional space using sinusoidal functions:
\begin{equation}
\gamma(p) = [\sin(2^0 \pi p), \cos(2^0 \pi p), \ldots, \sin(2^{L-1} \pi p), \cos(2^{L-1} \pi p)]
\end{equation}

For 3D coordinates, this transforms $\mathbf{x} \in \mathbb{R}^3$ to a $3 \times (1 + 2L)$-dimensional vector. Following the original NeRF, we use $L = 5$ frequencies, yielding 33-dimensional encodings.

Standard NeRF assumes a static scene. For articulated robots, the scene geometry changes with joint configuration. D-NeRF and subsequent deformation network variants extend NeRF to dynamic scenes by learning a canonical representation and a deformation field that maps observation-space points to canonical space \cite{pumarola2021d, park2021nerfies}. For robotic self-modeling, the time variable can be replaced with the joint configuration vector, learning a mapping from configuration space to deformation \cite{hu2024teaching}. Several acceleration techniques have been proposed to address NeRF's computational cost. Instant-NGP uses a multiresolution hash encoding that dramatically reduces the required MLP size, achieving real-time rendering for static scenes \cite{muller2022instant}. Mip-NeRF addresses aliasing by rendering anti-aliased conical frustums rather than rays, improving fidelity at the cost of additional computation \cite{barron2021mip}. Zip-NeRF further extends this with grid-based acceleration \cite{barron2023zipnerf}. However, even with acceleration, the core robotics bottleneck remains fundamentally unchanged: volumetric query expense remains a fundamental constraint within tight control loops.

\subsection{3D Gaussian Splatting: Explicit Geometric Primitives}
\label{subsec:3d_gaussian_splatting_explicit_geometric_primitives}

3D Gaussian Splatting (3DGS), introduced by Kerbl and colleagues, replaces implicit neural fields with explicit discrete geometric primitives \cite{kerbl20233d}. By abandoning the MLP entirely during inference and relying on highly optimized rasterization pipelines, 3DGS promises a pathway to real-time, high-fidelity scene representation. Unlike NeRF, which models a continuous field, 3DGS represents a scene as an unstructured point cloud of discrete 3D Gaussian primitives. Each individual Gaussian is mathematically defined by a 3D mean position $\boldsymbol{\mu}_i \in \mathbb{R}^3$, an anisotropic covariance matrix $\boldsymbol{\Sigma}_i \in \mathbb{R}^{3 \times 3}$ (parameterized by scaling $\mathbf{S}$ and rotation $\mathbf{R}$ via $\boldsymbol{\Sigma} = \mathbf{R} \mathbf{S} \mathbf{S}^\mathsf{T} \mathbf{R}^\mathsf{T}$), an opacity scalar $\alpha_i \in [0, 1]$, and spherical harmonics coefficients representing view-dependent directional color.

The core representational power of 3DGS lies in the anisotropy of the covariance matrix. A purely isotropic (spherical) Gaussian is severely limited in its ability to model sharp edges or flat surfaces. By allowing the covariance matrix to stretch and compress unevenly along its principal axes, a single Gaussian can deform into an elongated ellipsoid or a flattened disk, enabling precise modeling of complex geometry with fewer primitives \cite{kerbl20233d}. Optimizing a $3 \times 3$ covariance matrix directly using gradient descent is mathematically precarious, as the matrix must remain strictly positive semi-definite. To enforce this constraint, Kerbl and colleagues factorize the covariance matrix into a scaling vector $\mathbf{s} \in \mathbb{R}^3$ (with $s_i > 0$) and a rotation quaternion $\mathbf{q} \in \mathbb{R}^4$, then reparameterize the optimization to maintain positive semi-definiteness \cite{kerbl20233d}.

The true speed advantage of 3DGS is derived from its rendering pipeline. To synthesize an image, 3DGS completely abandons ray-marching, instead utilizing a highly parallelized tile-based rasterization architecture. For a given camera with viewing transformation $\mathbf{W}$ (world-to-camera) and projection $\mathbf{J}$ (Jacobian of the perspective projection), the projected 2D covariance $\boldsymbol{\Sigma}'$ is:
\begin{equation}
\boldsymbol{\Sigma}' = \mathbf{J} \mathbf{W} \boldsymbol{\Sigma} \mathbf{W}^\mathsf{T} \mathbf{J}^\mathsf{T}
\end{equation}

The 2D Gaussian function for pixel coordinates $\mathbf{u} \in \mathbb{R}^2$ is $G^{2D}(\mathbf{u}) = \exp\left(-\frac{1}{2} (\mathbf{u} - \boldsymbol{\mu}')^\mathsf{T} \boldsymbol{\Sigma}'^{-1} (\mathbf{u} - \boldsymbol{\mu}')\right)$, and the final pixel color is computed via alpha compositing with Gaussians sorted by depth:
\begin{equation}
C(\mathbf{u}) = \sum_{i=1}^{N} \alpha_i G_i^{2D}(\mathbf{u}) \mathbf{c}_i \prod_{j=1}^{i-1} (1 - \alpha_j G_j^{2D}(\mathbf{u}))
\end{equation}

The rapid adoption of 3DGS within robotics has enabled real-time SLAM systems. Gaussian Splatting SLAM \cite{matsuki2024gaussian} and SplaTAM \cite{keetha2024splattam} integrate 3DGS into SLAM pipelines, achieving real-time mapping and tracking with high-fidelity scene reconstruction. Surface regularization methods like SuGaR enable mesh extraction from Gaussian splats, bridging the gap between rendering and geometric simulation \cite{guedon2024sugar}. However, the computational cost scales with the number of Gaussians. For embedded deployment on edge devices like NVIDIA Jetson Orin, the parallel radix sort required for alpha-blending becomes memory-bandwidth bound, as shown by detailed benchmarking on mobile hardware \cite{lassanske2024gaurast}. The Jetson Orin operates under stricter power (15W typical) and memory bandwidth constraints (204~GB/s) compared to desktop GPUs, necessitating sparse, budgeted representations for self-repairing robots that must render hundreds of candidate morphologies per second.

\subsection{The Failure of Anisotropy on Robotic Morphologies}
\label{subsec:the_failure_of_anisotropy_on_robotic_morphologies}

Given these computational advantages, the robotics literature adapted 3DGS for kinematic self-modeling. The resulting architecture, Kinematic 3DGS (K-3DGS), attaches Gaussian primitives to a differentiable kinematic chain \cite{hu2025learning}. In theory, the anisotropic nature of 3DGS makes it ideal for robotic manipulators. The physical morphology of a serial manipulator is dominated by long, thin, rigid cylindrical links. An optimizer should naturally stretch the scaling vectors of Gaussians along the central longitudinal axis of each link, achieving aspect ratios of 20:1:1. In practice, however, this anisotropic optimization frequently fails catastrophically when applied to sparse, single-camera robotic datasets. Optimizing highly anisotropic rotational quaternions on featureless robotic links introduces a severely non-convex, ill-posed optimization landscape \cite{hu2025learning}. Because the robotic link is visually uniform, the gradient descent algorithm receives contradictory loss signals regarding the optimal rotation of the elongated Gaussian. This phenomenon is termed \textit{anisotropic mode collapse}.

To maintain numerical stability and ensure reliable convergence, practical K-3DGS implementations are frequently forced to retreat to purely isotropic representations. The scaling vector is constrained to be equal in all dimensions, forcing every Gaussian primitive to remain a perfect sphere. While this restriction mathematically stabilizes the training process, it fundamentally compromises the structural precision of the representation \cite{hu2025learning}. Attempting to model a long, sharp-edged robotic cylinder using only perfect spheres results in severe geometric aliasing. When rasterized, this isotropic chaining yields pervasive, structurally ambiguous ``blobby'' artifacts that blur the true spatial boundaries of the robot.

\subsection{Articulated Object Reconstruction for Digital Twin Generation}
\label{subsec:articulated_object_reconstruction_for_digital_twin_generation}

Automated digital twin generation approaches follow three distinct strategies reflecting different burden allocation decisions \cite{heiden2022inferring, weng2024neural, li2025art}. Differentiable reconstruction pipelines avoid manual URDF creation by optimizing directly from observations \cite{heiden2022inferring, weng2024neural}. Heiden and colleagues introduced a differentiable rendering pipeline that infers articulated rigid body dynamics directly from RGBD video, enabling the recovery of kinematic structure and joint parameters without hand-coding URDF files \cite{heiden2022inferring}. These methods use differentiable renderers to compare predicted images with observations, backpropagating error to update both geometry and articulation parameters. However, these methods are computationally expensive during optimization and may converge to local optima. Weng and colleagues extended this paradigm with neural implicit representations for building digital twins of unknown articulated objects, demonstrating reconstruction from sparse multi-view observations \cite{weng2024neural}.

Feed-forward generative models prioritize generation speed and simplicity over polish, deferring validation to downstream tasks. ART (Articulated Reconstruction Transformer) uses a transformer architecture to directly predict articulation parameters from multi-view images, achieving rapid reconstruction without per-object optimization \cite{li2025art}. URDF-Anything+ autoregressively generates URDF files from point clouds, enabling simulation-ready asset creation from depth sensors \cite{wu2025urdfanything}. ArtLLM leverages large language models for articulated asset generation, using semantic reasoning about part connectivity to infer kinematic structure \cite{wang2025artllm}. Kinematify enables open-vocabulary synthesis of high-DoF articulated objects through language-guided part decomposition \cite{wang2025kinematify}. These methods achieve rapid generation but may produce physically implausible geometries that fail under contact-rich manipulation.

Physics-constrained variants target specific failure modes like mesh interpenetration, adding domain knowledge at the cost of complexity. SIMART decomposes monolithic meshes into simulation-ready articulated assets using multimodal LLMs, explicitly checking for kinematic validity before deployment \cite{zhang2025simart}. MotionAnymesh provides physics-grounded articulation for simulation-ready digital twins, enforcing joint limit constraints and collision geometry consistency \cite{xu2025motionanymesh}. AOMGen generates photorealistic, physics-consistent demonstrations for articulated object manipulation, focusing on visual and dynamic realism \cite{wu2024aomgen}.

For manipulation tasks requiring closed-loop control, explicit world models offer an alternative to reconstruction-focused pipelines. Li and colleagues proposed building explicit world models for zero-shot open-world object manipulation, separating geometric reconstruction from dynamics prediction \cite{li2024explicit}. This decomposition enables the robot to reason about object geometry independently from task execution, improving generalization to novel environments. ArtGS extends this principle to 3D Gaussian splatting, providing interactive visual-physical modeling and manipulation of articulated objects with real-robot validation on Franka manipulators and cross-embodiment transfer to xArm-class systems \cite{yu2025artgs}. REArtGS++ achieves generalizable articulation reconstruction with temporal geometry constraints via planar Gaussian splatting, addressing occlusion challenges that degrade single-frame methods \cite{wu2025reartgs}.

A consistent limitation emerges across all three strategies: robustness properties fail acutely under realistic occlusion. Dense multi-object scenes with occlusion degrade inference sharply, exposing that single-model approaches lack robustness properties required for real deployment scenarios \cite{li2025art, zhang2025simart}. Multi-joint coupling spreads errors across morphologically related structures, and unconstrained capture generates visually plausible reconstructions that mask planning-critical geometric errors. These limitations underscore the value of the minimal representation principle demonstrated by NeuroKin: for control tasks requiring only silhouette-level awareness, the overhead of full 3D reconstruction may be unnecessary.

\subsection{Physics-Consistent Robot Models and Manipulation Planning}
\label{subsec:physics_consistent_robot_models_and_manipulation_planning}

Gaussian twin representations have progressed from display assets toward functional planning models. Robo-GS couples Gaussian representations with mesh extraction because simulators require solid geometry for accurate dynamics computation \cite{lou2024robogs}. ArticulatedGS enables self-supervised digital twin modeling of articulated objects using 3D Gaussian splatting \cite{guo2025articulatedgs}. ArtGS provides interactive visual-physical modeling and manipulation of articulated objects \cite{yu2025artgs}. REArtGS++ achieves generalizable articulation reconstruction with temporal geometry constraints via planar Gaussian splatting \cite{wu2025reartgs}. FreeArtGS extends articulated Gaussian splatting to free-moving scenarios \cite{dai2025freeartgs}. FreeGaussian enables annotation-free control of articulated objects via flow derivatives \cite{chen2025freegaussian}.

Planning stacks increasingly merge volumetric rendering with trajectory optimization for centimeter-level precision \cite{sun2026digitaltwin}. ManiGaussian uses dynamic Gaussian splatting for multi-task robotic manipulation \cite{lu2024manigaussian}. Predictive variants detect divergence between simulation and actual behavior, triggering corrective updates automatically when model accuracy degrades \cite{pan2025selfcorrecting}. GSWorld provides closed-loop photorealistic simulation for robotic manipulation \cite{jiang2025gsworld}. GaussGym offers an open-source real-to-sim framework for learning locomotion from pixels \cite{escontrela2025gaussgym}.

However, a persistent limitation emerges across published work: most papers demonstrate strong simulation metrics only over short horizons. Sensing robustness under environmental variation remains limited, latency jitter testing is absent, and contact uncertainty validation is incomplete \cite{lou2024robogs, pan2025selfcorrecting}. These three conditions---horizon length, environmental robustness, and contact fidelity---ultimately determine whether learned models generalize to deployment scenarios or remain confined to laboratory settings.

\subsection{Cross-Embodiment Transfer and Morphological Understanding}
\label{subsec:cross_embodiment_transfer_and_morphological_understanding}

Cross-embodiment transfer performance improves measurably when morphological structure is encoded as kinematic connectivity rather than visual appearance alone. DexGrasp-Zero aligns heterogeneous hand morphologies via semantic kinematic graphs, achieving zero-shot transfer in selected scenarios \cite{wu2025dexgrasp}. The generalization of embodiment-aware attention in GET-Zero is better than morphology-agnostic attention when dealing with unseen graphs and geometry variants \cite{patel2024getzero}. Success is driven by structural priors, not textures. Transfer fails when the kinematic structure differs substantially.

Whole-body control frameworks increasingly leverage these insights for humanoid deployment. AGILE provides a comprehensive workflow for humanoid loco-manipulation learning \cite{zhao2026agile}. Whole-body coordination frameworks couple base and arm dynamics for dynamic object grasping with legged manipulators \cite{liang2025wholebody}. Cybo-Waiter offers a physical agentic framework for humanoid whole-body locomotion-manipulation \cite{ren2026cybowaiter}. EAGLE uses embodiment-aware generalist-specialist distillation for unified humanoid whole-body control \cite{peng2026eagle}. HAIC enables humanoid agile object interaction control via dynamics-aware world models \cite{li2026haic}. These frameworks demonstrate that explicit structure priors substantially facilitate transfer across embodiments.

\subsection{Sim-to-Real Transfer via Digital Twins}
\label{subsec:sim_to_real_transfer_via_digital_twins}

Equating rendering fidelity with deployment success is a flawed assumption that has led to numerous simulation-only results failing on hardware \cite{aljalbout2026realitygap}. The reality gaps decompose along four distinct dimensions: dynamics mismatch (physics simulation divergence), perception mismatch (sensor modeling inaccuracy), actuation mismatch (motor characteristics divergence), and system construction mismatch (morphological/structural misalignment) \cite{aljalbout2026realitygap, tan2018simtoreal}. Effective transfer combines targeted system identification with methodical robustness training rather than pursuing perfect simulator fidelity. Hardware evidence supports this principle: quadrupeds trained with actuator-aware system identification and domain randomization achieve zero-shot transfer to real platforms \cite{tan2018simtoreal}. Bicycles trained with explicit robustness-focused procedures transfer reliably across diverse terrain \cite{liu2024cyclerl}. A consistent pattern emerges across successful systems: calibration without concurrent robustness training fails catastrophically, while robustness training without accurate baseline calibration merely wastes training data without achieving true transfer capability \cite{tan2018simtoreal, aljalbout2026realitygap}. Digital twins play multiple roles in sim-to-real pipelines as summarized in Table~\ref{tab:digital_twin_roles}.

\begin{table}[H]
\centering
\caption{Roles of digital twins in sim-to-real pipelines.}
\label{tab:digital_twin_roles}
\begin{tabular}{p{3.5cm}p{8cm}}
\toprule
\textbf{Role} & \textbf{Representative Works} \\
\midrule
Dynamics / system identification & \cite{tan2018simtoreal, heiden2022inferring, lou2026drex} \\
Perception / rendering transfer & \cite{qureshi2025splatsim, tang2025semantic} \\
Demonstration synthesis / augmentation & \cite{yang2025robosplat, yu2025real2render2real} \\
Deployment-time synchronization & \cite{abou2024realissim} \\
Evaluation / sim-real correspondence & \cite{jain2025polaris, li2024simpler} \\
\bottomrule
\end{tabular}
\end{table}

RoboSplat demonstrates concrete quantified impact: achieving 87.8\% task success compared to 57.2\% baseline, while simultaneously enabling 29$\times$ faster demonstration generation \cite{yang2025robosplat}. SplatSim enables zero-shot sim2real transfer of RGB manipulation policies using Gaussian splatting \cite{qureshi2025splatsim}. Real2Render2Real scales robot data without dynamics simulation or robot hardware \cite{yu2025real2render2real}. D-REX provides differentiable real-to-sim-to-real engine for learning dexterous grasping \cite{lou2026drex}. UniPR achieves simultaneous 100$\times$ speedup in scene processing and accuracy improvement for real-to-sim perception \cite{zhang2025unipr}. VR-Robo demonstrates successful RGB-only transfer in complex, previously unseen environments \cite{zhu2025vrrobo}. NavGSim shows that navigation policies trained in simulation transfer directly to real environments \cite{liu2025navgsim}.

\section{Damage Recovery and Fault Tolerance}

Damage recovery is where self-model claims face the most severe test: can the system operate with real faults, not just under nominal simulation conditions? This section surveys detection methods, recovery policy generation, humanoid-specific challenges, and practical deployment lessons.

\subsection{Detection Methods and Early Intervention}
\label{subsec:detection_methods_and_early_intervention}

Early fault detection methods are valuable only when directly tied to practical intervention latency and downstream control outcomes. VAE-based anomaly monitoring successfully detects anomalous force-torque trajectory patterns at sub-3-millisecond latencies in simulated manipulation tasks \cite{grambow2026anomaly}. Contrastive forward prediction reduces tracking error by up to 61.5\% on damaged legged machines \cite{fu2025contrastive}. Low-latency detection remains critical because delayed intervention necessarily closes the available recovery window, making response speed a fundamental constraint.

\subsection{Recovery Policy Generation and Adaptation}
\label{subsec:recovery_policy_generation_and_adaptation}

Detection must translate to actionable recovery policies. Vision-language recovery can guide task-level correction on 7-DoF manipulators; however, unconstrained language reasoning often violates dynamics limits \cite{chen2024vlm}. Structured recovery stacks provide a pragmatic alternative. Dream2Fix synthesizes counterfactual failures on a large scale (120,000+ simulation cases), achieving 46\% zero-shot closed-loop recovery on hardware \cite{li2025dream2fix}. Embodiment-conditioned diffusion policies achieve higher constraint satisfaction (36.85\% to 74.51\%) under zero-shot generalization to unseen failure conditions \cite{briscoe2025deft}.

\subsection{Humanoid Recovery and Stability}
\label{subsec:humanoid_recovery_and_stability}

Humanoid recovery introduces substantial additional complexity through stability and safety constraints not present in manipulator systems. Balance-informed critic networks grounded in capture point theory and centroidal dynamics computation achieve strong simulated stand-up recovery from falls \cite{poddar2026embedding}. Preference-conditioned control policies expand operational robustness envelopes by dynamically switching between efficiency-optimized and robustness-maximized control modes \cite{li2026pchc}. Fault-aware locomotion methods maintain stable tracking performance under joint faults within simulation \cite{lee2026tolebi}. Humanoid parkour research demonstrates impressive agile obstacle vaulting and long-horizon terrain traversal capabilities in controlled environments \cite{wu2026parkour}.

\subsection{Practical Deployment Lessons}
\label{subsec:practical_deployment_lessons}

Two practical lessons emerge consistently across successful deployment systems. First, online discrepancy monitoring proves effective when adaptation remains rapid and theoretically bounded, preventing unbounded divergence. World-model residual monitoring specifically enables rapid policy recovery following physical domain shifts like payload changes or link damage \cite{domberg2026selfadapting}. Second, residual adaptation channels remain most effective when carefully aligned with baseline policy safety constraints. This principle is demonstrated empirically: bounded residual control reduced Go1 quadruped recovery steps from 4,500 to 168 following a significant mass increase---a 96\% reduction in recovery time establishing the practical value of principled multi-level adaptation \cite{jayasinghe2026residual}. A consistent deployment principle emerges: recovery systems should be evaluated by intervention speed, stability margin, and post-fault task continuity---not by reconstruction fidelity. Visual quality is not the determining factor.

\section{Continual Learning for Self-Model Maintenance}

Continual maintenance of self-models confronts the stability-plasticity dilemma: updates must absorb new damage conditions while preserving knowledge of healthy morphology \cite{grossberg1987competitive, mermillod2013stability}. Catastrophic forgetting is well documented; in robotic deployment it is critical because forgetting can generate unsafe actions and hardware destruction \cite{mccloskey1989catastrophic, french1999catastrophic}.

\subsection{Balancing Plasticity and Stability}
\label{subsec:balancing_plasticity_and_stability}

Meta-learning approaches contribute meaningful value when morphology characteristics shift frequently and the time window available for adaptation compresses severely. Model-agnostic meta-learning (MAML) reduces post-damage sample requirements for model training \cite{finn2017maml}, and structure-constrained updates enable adaptation without full retraining. Elastic Weight Consolidation (EWC) mitigates catastrophic forgetting by approximating the posterior distribution of weights using the Fisher information matrix \cite{kirkpatrick2017overcoming}.

However, EWC is ill-suited for robotic damage recovery for two reasons. First, computational cost: calculating the Fisher Matrix requires computing second-order derivatives (Hessian) or approximating them via squared gradients. For high-dimensional rendering networks (millions of parameters), storing and computing the Fisher information is prohibitively expensive for real-time adaptation on edge hardware. Second, lack of modularity: EWC slows forgetting but does not enable the robot to isolate the damage. It treats the network as a monolith with varying ``stiffness''. If damage is severe, the robot needs to change high-importance weights, but EWC prevents this, leading to under-fitting of the damage (the ``plasticity gap'').

The failure of weight-based regularization suggests that topology, rather than weights, may hold the key to adaptive resilience. Research by Chechik and colleagues demonstrated through computational models that synaptic pruning is an optimal strategy strictly under metabolic or resource constraints \cite{chechik1998synaptic}. They proved that in the absence of such energetic costs, pruning cannot improve network performance compared to an intact network---a finding that aligns precisely with edge compute constraints in robotics, where power budgets (15W typical for Jetson Orin) and memory bandwidth are severely limited. The biological precedent suggests that sparse, modular architectures may be not merely beneficial but necessary for resource-constrained adaptive systems.

\subsection{Resource-Efficient Adaptation via Sparsity}
\label{subsec:resource_efficient_adaptation_via_sparsity}

The Lottery Ticket Hypothesis formalizes the value of sparsity in deep learning: ``A randomly-initialized dense neural network contains a subnetwork that is initialized such that---when trained in isolation---it can match the test accuracy of the original network after training for at most the same number of iterations'' \cite{frankle2018lottery}. This suggests that the vast majority of weights in a dense self-model are redundant. Han and colleagues empirically demonstrated this, achieving compression ratios of 49$\times$ on AlexNet and 39$\times$ on VGG-16 through a combination of pruning, quantization, and Huffman coding \cite{han2015deep}. Model storage reduced from 240~MB to 6.9~MB while maintaining baseline accuracy \cite{han2015deep}.

Sparse Evolutionary Training (SET) maintains sparse topology throughout training rather than training dense and pruning post-hoc \cite{mocanu2018scalable}. In each epoch, SET prunes the weakest connections and regrows random new ones, allowing the network to explore the topology space dynamically. For robotics, sparsity offers dual benefits. First, efficiency: sparse matrix operations reduce computational latency, directly addressing the rendering bottleneck. Second, modularity: by enforcing sparsity, the network is forced to develop modular sub-structures. Damage to one module can be repaired by updating only the weights within that module, significantly reducing catastrophic interference \cite{frankle2018lottery, mocanu2018scalable}. This biological inspiration---sparse, modular connectivity enabling localized plasticity---motivates our architectural explorations in subsequent sections.

\subsection{Safety-Constrained and Bounded Adaptation}
\label{subsec:safety_constrained_and_bounded_adaptation}

Safety-constrained learning maintains adaptation within verified regions through explicit constraints. Barrier functions enforce that the robot stays within safe regions of state space, providing formal safety guarantees \cite{brunke2021safe}. System-level state partitioning separates high-level reasoning from time-critical control loops, reducing cascading failures. Bounded residual control keeps adaptations within safe envelopes, as demonstrated empirically by the 96\% reduction in recovery time on Go1 quadruped \cite{jayasinghe2026residual}.

\section{Theoretical Foundations of Swarm Coordination and Temporal Stigmergy}

This section establishes the theoretical framework of temporal stigmergy that underpins the sequential swarm evaluation in Chapter 6, drawing on biological inspiration and multi-agent systems research.

\subsection{Stigmergy: Biological and Algorithmic Foundations}
\label{subsec:stigmergy_biological_and_algorithmic_foundations}

Stigmergy is a biological mechanism of indirect coordination, originally observed in social insects such as termites and ants. Individual agents do not communicate directly via explicit signaling protocols or negotiated consensus algorithms. Instead, they subtly alter the physical environment---for example, depositing a pheromone trail or adding a pellet of construction material. These environmental modifications serve as localized, persistent stimuli, triggering specific behavioral responses from subsequent agents that encounter them. The critical property of stigmergy is that coordination emerges from local interactions without centralized planning or direct agent-to-agent communication. The environment itself becomes the communication channel. This property makes stigmergy particularly attractive for robotic swarms operating in communication-denied environments where radio bandwidth is limited or connectivity is intermittent.

Translated into algorithmic robotic systems, stigmergy eliminates the need for continuous peer-to-peer communication bandwidth. Agents react independently and locally to diagnostic traces left in the environment by their peers \cite{bongard2006resilient}. This stands in contrast to consensus algorithms that require all-to-all communication ($O(N^2)$ messages) and multi-agent reinforcement learning approaches that require centralized training and substantial computational resources \cite{domberg2026selfadapting, daneshmand2026slowrl}.

\subsection{Temporal Stigmergy in Sequential Pipelines}
\label{subsec:temporal_stigmergy_in_sequential_pipelines}

In this thesis, stigmergic coordination is achieved across time rather than physical space. The shared ``environment'' is abstracted into a persistent digital memory ledger---the \texttt{factory\_state} variable tracked across sequential agents. This represents a \textit{temporal stigmergy}: agents coordinate by reading and writing to a shared state that persists across sequential operations, with the dimension of coordination being time rather than space. Formally, we define temporal stigmergy as a coordination mechanism where agents $\{A_1, A_2, \ldots, A_N\}$ share a persistent state $S \in \mathcal{S}$. Each agent $A_i$ executes a protocol beginning by reading the current state $S_{i-1}$ (the state after the previous agent's execution). The agent then selects behavior $B_i = \pi(S_{i-1})$ according to a policy $\pi: \mathcal{S} \rightarrow \mathcal{B}$. After executing the behavior and observing outcome $O_i \in \{\text{success}, \text{failure}\}$, the agent updates the state according to $S_i = \Phi(S_{i-1}, O_i)$ where $\Phi$ is the state transition function.

Coordination emerges from the sequential application of $\pi$ and $\Phi$ without any direct communication between agents beyond the state $S$ that persists in the shared environment. When an agent experiences repeated mechanical failures, it updates the \texttt{consecutive\_failures} integer. Subsequent agents read this trace. If the trace indicates a degraded operational history (two consecutive failures), the new agent autonomously shifts into a localized ``recovery'' mode: it abandons its randomized spatial target, moves to a conservative centralized coordinate, and doubles its Cartesian error tolerance. This state-dependent, history-aware behavioral adaptation is the core logic of decentralized swarm coordination.

\subsection{Fast Inference as a Swarm Prerequisite}
\label{subsec:fast_inference_as_a_swarm_prerequisite}

A swarm can only coordinate around diagnostic states if individual agents possess the capability to produce those states reliably and in a timely manner. If an agent's internal self-model requires hundreds of milliseconds per inference, it cannot reliably determine whether a failure was caused by occlusion, oscillation, or morphological drift---by the time the diagnosis completes, the system state may have changed. Our empirical evaluations show that NeuroKin provides accurate visual self-diagnosis with high throughput, establishing the computational prerequisite for swarm endurance. Fast inference enables three properties essential for stigmergic coordination: real-time failure detection (the agent knows within a single control cycle whether it has succeeded or failed); immediate ledger updates (the shared state is updated before the next control cycle begins); and rapid adaptation (subsequent agents read the updated state without perceptible delay). Without fast inference, temporal stigmergy would introduce latency proportional to the number of agents, causing the coordination mechanism to become the bottleneck.

\section{Summary of Literature Findings}

This exhaustive literature synthesis reveals several critical findings that directly motivate the experimental contributions of this thesis.
\textbf{First}, visual self-modeling via neural rendering (NeRF and 3DGS) has achieved impressive reconstruction fidelity, with reported PSNR values exceeding 23~dB on benchmark datasets. However, the field has systematically under-emphasized the latency implications of volumetric reconstruction for real-time control. The reported frame rates (5--37~FPS) are fundamentally incompatible with the 1~kHz (1,000~Hz) control loops required for dynamic robotic systems. This latency paradox represents a structural mismatch, not an engineering optimization problem.
\textbf{Second}, direct sensorimotor decoding---bypassing explicit 3D reconstruction entirely---has been explored in limited contexts but never systematically compared against volumetric approaches on controlled benchmarks. The theoretical advantage of dense gradient supervision suggests that direct decoding could achieve both higher speed and better quality, but this hypothesis requires rigorous empirical validation.
\textbf{Third}, continual learning for self-model maintenance faces unresolved challenges around catastrophic forgetting. Existing approaches (EWC, sparse subnetworks, mixture-of-experts) offer partial solutions but have not been validated in damage recovery scenarios where the distribution shifts abruptly and safety constraints dominate. The biological precedent of synaptic pruning under resource constraints suggests that sparse, modular architectures may be necessary for edge-deployable adaptive systems.
\textbf{Fourth}, digital twin frameworks for robotics have emphasized geometric fidelity and simulation accuracy but have neglected the temporal dimension of self-modeling: the twin must be queryable at control rates to be useful for online adaptation. This temporal requirement aligns with the latency findings from neural rendering analysis. Recent work on Robo-GS, ArticulatedGS, and deformable simulation pipelines demonstrates progress, but the evaluation gap---simulation metrics over short horizons versus deployment metrics over extended operation---remains unresolved.
\textbf{Fifth}, stigmergic coordination offers a promising framework for decentralized multi-agent fault tolerance, but the prerequisite---fast, reliable individual self-diagnosis---has not been demonstrated in prior work. Temporal stigmergy, as formalized in this thesis, provides a theoretical bridge from biological inspiration to algorithmic implementation, with the critical enabling condition being fast inference.
\textbf{Sixth}, across all surveyed literature, a consistent evaluation gap emerges: most papers report reconstruction metrics (PSNR, SSIM, LPIPS) but not task-level outcomes (success rate, recovery time, stability margins). This disconnect between diagnostic metrics and deployment metrics limits the field's ability to assess practical readiness. Hardware validation remains the gold standard, yet simulation-only results continue to dominate the literature.

\section{Research Questions Grounded in Literature}

Based on the literature synthesis, four research questions guide the experimental investigation. These questions emerge directly from identified gaps rather than being imposed a priori.

\subsection{RQ1: Architectural Efficiency}
\label{subsec:rq1_architectural_efficiency}

To what extent can the computational latency of visual robot self-modeling be reduced by replacing 3D implicit and explicit volumetric rendering paradigms with 2D direct sensorimotor decoding, and what is the resulting impact on morphological reconstruction fidelity measured in PSNR? This question probes the fundamental architectural trade-off that the literature has systematically under-explored. The hypothesis is that dense gradient supervision from direct pixel prediction will achieve both higher speed and better quality than sparse volumetric sampling.

\subsection{RQ2: Control Integration}
\label{subsec:rq2_control_integration}

Can a visual self-model operating purely on direct convolutional decoding provide spatial coordinate estimates with sufficient accuracy and fast inference to actively stabilize a Jacobian-based inverse kinematics control loop? This question moves beyond static rendering metrics to dynamic closed-loop performance, asking whether the network's predictions are smooth and accurate enough to serve as the primary state estimate for feedback control---a capability not demonstrated for any neural rendering self-model to date.

\subsection{RQ3: Fault Tolerance}
\label{subsec:rq3_fault_tolerance}

How does the integration of fast visual self-estimates alter end-effector trajectory and error convergence compared to a purely proprioceptive baseline under induced mechanical faults such as encoder slip? This question evaluates the system's resilience under the very conditions that motivate self-modeling in the first place: when physical damage corrupts proprioceptive readings, can visual estimates override them and maintain safe, stable control? The literature on bounded residual control and world-model monitoring suggests that rapid adaptation is possible, but the prerequisite fast inference has not been demonstrated.

\subsection{RQ4: Collective Adaptation}
\label{subsec:rq4_collective_adaptation}

Can the individual computational efficiency of a fast self-model enable decentralized, state-dependent adaptation across a sequential pipeline of agents, thereby validating the temporal stigmergy mechanics required for future multi-agent swarm coordination? This question scales the investigation from single-agent survival to collective endurance, testing whether fast individual self-diagnosis can support emergent coordination without explicit inter-agent communication. The information-theoretic efficiency of stigmergy---coordination with O(1) bits per agent---makes it particularly attractive for bandwidth-constrained swarms.
\clearpage

% ==========================================
% CHAPTER 3: BASELINE IMPLEMENTATIONS AND THE LATENCY BOTTLENECK
% ==========================================
\chapter{Baseline Implementations and the Latency Bottleneck}
\label{chap:baseline_latency_bottleneck}

To rigorously character-profile the latency paradox motivating our structural modifications, we execute a thorough independent reimplementation and benchmark evaluation of two dominant modern neural self-modeling paradigms. The first is the Free-Form Kinematic Self-Model (FFKSM) anchored on deep coordinate implicit network structures \cite{hu2024teaching}, and the second is Kinematic 3D Gaussian Splatting (K-3DGS) built upon explicit graphics primitives \cite{hu2025learning}. Each baseline framework is tested within an identical validation pipeline, consuming the same training arrays and running on equivalent GPU hardware configurations.

\section{Synthetic Data Generation via Lorenz Attractors}

All experiments use a dataset of 2,000 samples generated from a PyBullet simulation of a 4-DOF serial manipulator. This section details the complete data generation pipeline, including simulation configuration, trajectory planning, and preprocessing procedures.

\subsection{Simulation Environment Configuration}
\label{subsec:simulation_environment_configuration}

The PyBullet physics simulator provides a controlled environment for generating ground-truth robot observations. We configure the simulation with the following parameters:
\begin{itemize}
    \item \textbf{Robot Model}: A 4-degree-of-freedom serial manipulator with revolute joints. The base is fixed at the origin with joint axes arranged in an anthropomorphic configuration: joint 1 rotates about the vertical axis (yaw), joints 2 and 3 rotate about horizontal axes (pitch), and joint 4 provides wrist rotation.
    \item \textbf{Camera Setup}: A fixed RGB camera positioned at coordinates $[1.0, 0.0, 0.0]$ with focal length 130.2545. The camera looks toward the origin, producing $100 \times 100$ pixel images with a field of view of 42 degrees.
    \item \textbf{Rendering}: The simulation runs in DIRECT mode (no graphical display) for maximum speed. Each step renders the robot using PyBullet's built-in tiny renderer with shadows disabled.
    \item \textbf{Physics}: Gravity is set to $-9.8 \, \text{m/s}^2$ in the z-direction. A ground plane is placed at $z = -0.109$~m. Each joint is position-controlled with a maximum force of 100~N.
\end{itemize}

Figure~\ref{fig:pybullet_setup} shows the simulation scene and camera placement used for data generation.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{pybullet_setup.png}
    \caption{PyBullet simulation environment setup: 4-DOF articulated robot arm with fixed overhead camera and ground plane for data acquisition.}
    \label{fig:pybullet_setup}
\end{figure}

The robot's kinematic structure follows the Denavit-Hartenberg (DH) convention. The DH parameters are:
\begin{table}[H]
\centering
\caption{Denavit-Hartenberg parameters for the 4-DOF manipulator.}
\label{tab:dh_parameters}
\begin{tabular}{ccccc}
\toprule
Link $i$ & $\alpha_{i-1}$ (rad) & $a_{i-1}$ (m) & $d_i$ (m) & $\theta_i$ (var) \\
\midrule
1 & 0 & 0 & 0.1 & $\theta_1$ \\
2 & $\pi/2$ & 0 & 0 & $\theta_2$ \\
3 & 0 & 0.4 & 0 & $\theta_3$ \\
4 & 0 & 0.4 & 0 & $\theta_4$ \\
\bottomrule
\end{tabular}
\end{table}

The forward kinematics transformation from base to end-effector is given by the product of individual link transformations:
\begin{equation}
T_{0}^{4} = T_{0}^{1}(\theta_1) T_{1}^{2}(\theta_2) T_{2}^{3}(\theta_3) T_{3}^{4}(\theta_4)
\end{equation}

where each $T_{i-1}^{i}$ is computed using the standard DH convention:
\begin{equation}
T_{i-1}^{i} = 
\begin{bmatrix}
\cos\theta_i & -\sin\theta_i & 0 & a_{i-1} \\
\sin\theta_i \cos\alpha_{i-1} & \cos\theta_i \cos\alpha_{i-1} & -\sin\alpha_{i-1} & -d_i \sin\alpha_{i-1} \\
\sin\theta_i \sin\alpha_{i-1} & \cos\theta_i \sin\alpha_{i-1} & \cos\alpha_{i-1} & d_i \cos\alpha_{i-1} \\
0 & 0 & 0 & 1
\end{bmatrix}
\end{equation}

\subsection{Lorenz Attractor Trajectory Generation}
\label{subsec:lorenz_attractor_trajectory_generation}

Rather than relying on random motor babbling---which produces jerky, discontinuous motions that poorly sample the workspace---we employ trajectories generated by the Lorenz dynamical system. The Lorenz attractor is a system of three coupled ordinary differential equations (ODEs) that exhibits deterministic chaos, producing trajectories that are smooth, non-repeating, and ergodic within a bounded region of state space. The Lorenz system is defined as~\cite{lorenz1963}:
\begin{align}
\frac{dx}{dt} &= \sigma(y - x) \\
\frac{dy}{dt} &= x(\rho - z) - y \\
\frac{dz}{dt} &= xy - \beta z
\end{align}

The parameter values determine the dynamical behavior. We use the classical values $\sigma = 10$, $\rho = 28$, $\beta = 8/3$, which place the system in the chaotic regime. At these parameters, the Lyapunov exponent is positive, ensuring sensitive dependence on initial conditions and exponential divergence of nearby trajectories. The system has two unstable fixed points at $(\pm\sqrt{\beta(\rho-1)}, \pm\sqrt{\beta(\rho-1)}, \rho-1)$ and a stable fixed point at the origin (which is only stable for $\rho < 1$). For $\rho = 28$, the origin is a saddle point, and trajectories are attracted to a strange attractor---a fractal set of measure zero with non-integer Hausdorff dimension.

To numerically integrate the Lorenz system, we employ the fourth-order Runge-Kutta (RK4) method. For a general ODE $\frac{d\mathbf{x}}{dt} = \mathbf{f}(\mathbf{x}, t)$, the RK4 update from state $\mathbf{x}_n$ at time $t_n$ to $\mathbf{x}_{n+1}$ at time $t_{n+1} = t_n + \Delta t$ is:
\begin{align}
\mathbf{k}_1 &= \mathbf{f}(\mathbf{x}_n, t_n) \\
\mathbf{k}_2 &= \mathbf{f}(\mathbf{x}_n + \frac{\Delta t}{2}\mathbf{k}_1, t_n + \frac{\Delta t}{2}) \\
\mathbf{k}_3 &= \mathbf{f}(\mathbf{x}_n + \frac{\Delta t}{2}\mathbf{k}_2, t_n + \frac{\Delta t}{2}) \\
\mathbf{k}_4 &= \mathbf{f}(\mathbf{x}_n + \Delta t \mathbf{k}_3, t_n + \Delta t) \\
\mathbf{x}_{n+1} &= \mathbf{x}_n + \frac{\Delta t}{6}(\mathbf{k}_1 + 2\mathbf{k}_2 + 2\mathbf{k}_3 + \mathbf{k}_4)
\end{align}

RK4 is a fourth-order method, meaning the local truncation error is $O(\Delta t^5)$ and the global error is $O(\Delta t^4)$. This provides excellent stability and accuracy for the Lorenz system, which is stiff in certain regions of state space.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{lorenz_trajectories.png}
    \caption{Trajectory generation via coupled Lorenz attractors: four joint angle profiles over 2000 samples, demonstrating smooth, ergodic workspace coverage.}
    \label{fig:lorenz_trajectories}
\end{figure}

We set the integration time step to $\Delta t = 0.01$~seconds. This value was chosen through systematic experimentation: larger steps ($\Delta t \geq 0.02$) caused numerical instability and trajectory divergence, while smaller steps ($\Delta t \leq 0.005$) increased computational cost without measurable improvement in trajectory quality. After each RK4 step, the raw Lorenz state values are scaled using the hyperbolic tangent function:
\begin{equation}
\text{action} = \tanh(\alpha \cdot \mathbf{x})
\end{equation}

where $\alpha = 0.025$ is a scaling factor. The $\tanh$ function maps the unbounded Lorenz state to the bounded interval $[-1, 1]$, which corresponds to the normalized joint angle range. This scaling preserves the sign and relative magnitude of variations while ensuring the command stays within valid limits. Two independent Lorenz systems drive the four robot joints. Specifically:
\begin{itemize}
    \item Joint 1 follows $x$ component of trajectory 1
    \item Joint 2 follows $y$ component of trajectory 1
    \item Joint 3 follows $x$ component of trajectory 2
    \item Joint 4 follows $z$ component of trajectory 2
\end{itemize}
Using two independent attractors (with slightly different time steps: $\Delta t_1 = 0.01$, $\Delta t_2 = 0.012$) ensures that the four joints are not perfectly correlated, producing a richer diversity of poses.

\subsection{Data Collection and Preprocessing}
\label{subsec:data_collection_and_preprocessing}

For each of the 2,000 samples, the data collection procedure follows:
\begin{enumerate}
    \item The robot is reset to a neutral configuration (all joints at 0 degrees).
    \item A target joint angle vector $\mathbf{q} \in [-90^\circ, +90^\circ]^4$ is computed from the Lorenz trajectories.
    \item Position controllers move each joint to the target angle over 50 simulation steps (equivalent to 200~ms of physical time), allowing the robot to settle.
    \item An RGB image is captured from the fixed camera.
    \item Joint angles are recorded from the simulation state.
\end{enumerate}

The RGB images are preprocessed to produce binary silhouette masks:
\begin{enumerate}
    \item Convert RGB to grayscale: $I_{\text{gray}} = 0.299R + 0.587G + 0.114B$
    \item Apply threshold: $I_{\text{mask}}(u,v) = \begin{cases} 1 & \text{if } I_{\text{gray}}(u,v) < 240 \\ 0 & \text{otherwise} \end{cases}$
    \item The threshold value 240 was empirically determined to optimally separate robot pixels (typically lighter due to white/gray coloring) from background (darker).
\end{enumerate}

The dataset comprises 2,000 samples split into 1,600 training samples (80\%) and 400 validation samples (20\%). The split is performed randomly with a fixed seed (42) for reproducibility.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{data_pipeline.png}
    \caption{Data preprocessing pipeline: RGB image acquisition from base raw RGB image, grayscale conversion, and binary segmentation to produce segmented silhouette masks.}
    \label{fig:data_pipeline}
\end{figure}

\subsection{Dataset Statistics and Quality Analysis}
\label{subsec:dataset_statistics_and_quality_analysis}

Statistical analysis confirms good workspace coverage:
\begin{itemize}
    \item Joint angle means: $-0.23^\circ$ to $12.45^\circ$ (near zero, as expected for a system centered at neutral)
    \item Joint angle standard deviations: approximately $30^\circ$ (indicating broad exploration)
    \item Robot pixel occupancy: average 14.7\%, range 8.9\%--23.4\%
    \item Image dimensions: $100 \times 100$ pixels (10,000 total pixels per image)
\end{itemize}

The low average occupancy (14.7\%) creates severe class imbalance---85\% of pixels are background (0) and only 15\% are robot (1). This imbalance creates a pathological convergence trap for naive volumetric rendering, as the network can achieve low loss by simply predicting all pixels as 0. This phenomenon, which we term \textit{black-screen convergence}, is detailed in Section~\ref{subsubsec:challenge_1_black_screen_convergence}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{sample_poses.png}
    \caption{Representative single-view training samples: 4x2 structured image grid showing 8 unique spatial joint configurations generated for the single view dataset.}
    \label{fig:sample_poses}
\end{figure}

\section{Free-Form Kinematic Self-Model (FFKSM): Implicit Neural Rendering}

The FFKSM architecture, introduced by Hu, Lin, and Lipson \cite{hu2024teaching}, adapts Neural Radiance Fields (NeRF) \cite{mildenhall2020nerf} to articulated robot self-modeling. Unlike traditional NeRF, which assumes static scenes, FFKSM conditions the radiance field on joint angles, enabling dynamic rendering as the robot moves.

\subsection{Core Mathematical Framework}
\label{subsec:core_mathematical_framework}

\subsubsection{Neural Radiance Fields for Static Scenes}
\label{subsubsec:neural_radiance_fields_for_static_scenes}

NeRF represents a static 3D scene as a continuous function $F_\theta: (\mathbf{x}, \mathbf{d}) \rightarrow (\mathbf{c}, \sigma)$ parameterized by a multi-layer perceptron (MLP) with weights $\theta$. The function maps a 3D spatial position $\mathbf{x} \in \mathbb{R}^3$ and a viewing direction $\mathbf{d} \in \mathbb{R}^3$ (normalized to unit length) to an emitted color $\mathbf{c} \in \mathbb{R}^3$ (RGB values in $[0,1]^3$) and a volume density $\sigma \in \mathbb{R}^+$. To render a pixel, NeRF approximates the volume rendering integral along a camera ray $\mathbf{r}(t) = \mathbf{o} + t\mathbf{d}$, where $\mathbf{o}$ is the camera origin and $t \in [t_n, t_f]$ ranges from near plane to far plane. The expected color $C(\mathbf{r})$ is:
\begin{equation}
C(\mathbf{r}) = \int_{t_n}^{t_f} T(t) \sigma(\mathbf{r}(t)) \mathbf{c}(\mathbf{r}(t), \mathbf{d}) \, dt
\end{equation}

where $T(t)$ is the accumulated transmittance along the ray from $t_n$ to $t$:
\begin{equation}
T(t) = \exp\left(-\int_{t_n}^{t} \sigma(\mathbf{r}(s)) \, ds\right)
\end{equation}

In practice, this continuous integral is approximated via numerical quadrature using stratified sampling. Along each ray, $N$ points $\{t_i\}_{i=1}^N$ are sampled, and the discrete approximation is:
\begin{equation}
\hat{C}(\mathbf{r}) = \sum_{i=1}^{N} T_i (1 - \exp(-\sigma_i \delta_i)) \mathbf{c}_i
\end{equation}

where $\delta_i = t_{i+1} - t_i$ is the distance between adjacent samples, and $T_i = \exp\left(-\sum_{j=1}^{i-1} \sigma_j \delta_j\right)$. For a $100 \times 100$ image with $N = 64$ samples per ray, this requires $100 \times 100 \times 64 = 640,000$ network evaluations per frame. This computational burden is the fundamental bottleneck that prevents real-time control integration.

\subsubsection{Positional Encoding}
\label{subsubsec:positional_encoding}

Standard MLPs exhibit a spectral bias toward low-frequency functions \cite{rahaman2019spectral}. To capture high-frequency geometric details (e.g., sharp edges of robot links), NeRF employs positional encoding that maps input coordinates to a higher-dimensional space using sinusoidal functions:
\begin{equation}
\gamma(p) = [\sin(2^0 \pi p), \cos(2^0 \pi p), \ldots, \sin(2^{L-1} \pi p), \cos(2^{L-1} \pi p)]
\end{equation}

For 3D coordinates, this transforms $\mathbf{x} \in \mathbb{R}^3$ to a $3 \times (1 + 2L)$-dimensional vector. Following the original NeRF, we use $L = 5$ frequencies, yielding 33-dimensional encodings.

\subsubsection{FFKSM Adaptation for Articulated Robots}
\label{subsubsec:ffksm_adaptation_for_articulated_robots}

FFKSM extends NeRF to articulated robots through two key innovations: the \textit{virtual frame transformation} and the \textit{split encoder architecture}.

\textbf{Virtual Frame Transformation:} The virtual frame transforms 3D query points using the first two joint angles ($\theta_0, \theta_1$) before network processing:
\begin{equation}
\mathbf{x}_{\text{virtual}} = R_z(-\theta_0) R_y(-\theta_1) \mathbf{x}_{\text{world}}
\end{equation}

where $R_z$ and $R_y$ are rotation matrices:
\begin{equation}
R_z(\alpha) = \begin{bmatrix}
\cos\alpha & -\sin\alpha & 0 \\
\sin\alpha & \cos\alpha & 0 \\
0 & 0 & 1
\end{bmatrix}, \quad
R_y(\beta) = \begin{bmatrix}
\cos\beta & 0 & \sin\beta \\
0 & 1 & 0 \\
-\sin\beta & 0 & \cos\beta
\end{bmatrix}
\end{equation}

This transformation reduces the network's learning burden by aligning coordinates with the robot's base orientation, effectively factoring out base rotation from the end-effector position learning problem. Without this prior, the network would need to learn circular symmetry---a challenging task for MLPs.

\textbf{Split Encoder Architecture:} FFKSM separates coordinate processing from kinematic processing:
\begin{enumerate}
    \item \textbf{Coordinate Encoder}: Takes virtual-frame 3D points via positional encoding (5 frequencies $\rightarrow$ 33 dimensions) followed by a two-layer MLP (128 hidden units each).
    \item \textbf{Kinematic Encoder}: Processes the remaining two joint angles ($\theta_2, \theta_3$) via separate positional encoding ($2 \times 5$ frequencies $\rightarrow$ $2 \times 11 = 22$ dimensions) and an identical two-layer MLP (128 hidden units).
    \item \textbf{Predictive Module}: Concatenates the 128-dimensional features from both encoders (256 total) and passes through four linear layers ($128 \rightarrow 128 \rightarrow 32 \rightarrow 2$) to predict density $\sigma$ and visibility $v$.
\end{enumerate}

The split architecture is critical: a single encoder processing $(\mathbf{x}, \theta)$ jointly would need to relearn the kinematic transformation for every 3D point. The split design allows the kinematic encoder to learn the mapping $\theta \rightarrow \text{link transformation}$ once, then apply it to all query points, dramatically improving sample efficiency.

\subsection{Implementation Details and Reproduction Challenges}
\label{subsec:implementation_details_and_reproduction_challenges}

Our independent implementation of FFKSM from scratch uncovered three critical challenges not detailed in the original publication. Each challenge required significant debugging and architectural modifications.

\subsubsection{Challenge 1: Black-Screen Convergence}
\label{subsubsec:challenge_1_black_screen_convergence}
\textbf{Symptom:} Initial training exhibited a pathological failure mode: the network converged to predicting uniformly black images (all zeros) despite achieving apparently low loss values. The loss decreased smoothly, suggesting successful optimization, but qualitative inspection revealed complete failure.

\textbf{Root Cause Analysis:} Robots occupy only approximately 15\% of image pixels, creating severe class imbalance (85\% background, 15\% robot). The network discovered a local minimum where predicting the majority class (black) achieved mean squared error of $\sim 0.85$, yet produced no useful representation. This local minimum is extremely attractive because the gradient magnitude for background pixels (which are correctly predicted) is near zero, the gradient from robot pixels is sparse and weak, and the optimizer follows the path of least resistance, settling into the trivial solution. This failure persisted for the first 1,000 iterations, rendering standard training completely ineffective.

\textbf{Attempted Solutions Narrative:} In trying to resolve this early convergence failure, several loss modifications were methodically explored. Proportional weighted losses scaling the backpropagation updates exclusively on the robot boundaries were injected into the optimizer. Applying a conservative $2\times$ and $3\times$ scaling factor to the sparse foreground coordinates provided insufficient signal to pull the gradients out of the background local minimum. Elevating the foreground multiplier to $5\times$ yielded initial trace improvements but introduced severe numerical boundary instability. When scaling targets were further pushed to $10\times$ and $20\times$, the optimizer destabilized immediately, producing volatile oscillations and rapid network mode collapse. In parallel, a structural focal loss configuration was implemented to explicitly down-weight easy background classifications. This method introduced complex hyperparameter sweeps over focus bounds ($\gamma, \alpha$) that proved highly sensitive, ultimately failing to break the all-zero baseline attractor. Similarly, intersection-over-union optimizations via a continuous Dice loss metric were evaluated; however, because early epoch robot tracking was sparse, the fractional loss boundaries fluctuated wildly and generated catastrophic gradient explosions.

\textbf{Solution: Center-Cropping Curriculum Learning}

We solved the black-screen convergence through a structured curriculum learning approach. The key insight is to eliminate the class imbalance during critical early learning by focusing exclusively on image regions where robot occupancy is high. For the first $N_{\text{crop}} = 500$ iterations, training focuses on the central $50 \times 50$ region where robot occupancy exceeds 40\%. After this period, training expands to the full $100 \times 100$ image. The curriculum can be formalized as a time-varying loss mask:
\begin{equation}
\mathcal{L}_t = \begin{cases}
\frac{1}{50^2} \sum_{(i,j) \in \text{center}} w(I_{ij}^{\text{GT}}) \|I_{ij}^{\text{pred}} - I_{ij}^{\text{GT}}\|^2, & t < 500 \\
\frac{1}{100^2} \sum_{i=1}^{100}\sum_{j=1}^{100} w(I_{ij}^{\text{GT}}) \|I_{ij}^{\text{pred}} - I_{ij}^{\text{GT}}\|^2, & t \geq 500
\end{cases}
\end{equation}

where $w(I_{ij}^{\text{GT}}) = 5$ for robot pixels ($I_{ij}^{\text{GT}} = 1$) and $1$ for background pixels ($I_{ij}^{\text{GT}} = 0$). The center crop dimensions ($50 \times 50$) were chosen because at this crop size, robot occupancy consistently exceeds 40\% across all training samples, the crop is large enough to capture the entire robot's articulation range, and it eliminates the class imbalance that causes black-screen convergence. The transition point (500 iterations) was empirically determined: earlier transitions (<300 iterations) reintroduced the black-screen problem, while later transitions (>800 iterations) slowed overall convergence without benefit.

\subsubsection{Challenge 2: Kinematic Blindness}
\label{subsubsec:challenge_2_kinematic_blindness}
\textbf{Symptom:} After apparent convergence at iteration 2,000, all metrics indicated success: PSNR improved from 6.94~dB to 17.35~dB, validation loss decreased smoothly from 0.24 to 0.023, and training curves appeared stable. However, qualitative validation revealed a subtle but devastating bug: all poses with identical base orientation ($\theta_0, \theta_1$) produced identical predictions regardless of end-effector configuration ($\theta_2, \theta_3$). The network exhibited "kinematic blindness" to distal joints, essentially modeling only the robot's base as a rigid cylinder.

\textbf{Root Cause Analysis:} Gradient flow analysis using PyTorch's autograd hooks uncovered the root cause: a tensor slicing bug where only $(\theta_0, \theta_1$) reached the kinematic encoder. The erroneous code was:
\begin{verbatim}
kin_input = joints[:,2:]  % Erroneously sliced all remaining dimensions
\end{verbatim}

This single-character indexing error (using `2:` instead of `2:4`) caused the tensor slice to include all remaining dimensions from index 2 onward. However, in our data pipeline, the joint tensor had shape \texttt{(batch\_size, 4)}, so \texttt{joints[:,2:]} extracted columns 2 and 3---fortuitously the correct values. The bug appeared only when we modified the data pipeline for diagnostic experiments, exposing the fragility of implicit indexing. More fundamentally, the bug revealed an architectural vulnerability: the split encoder design assumes fixed semantics for joint indices (first two for transformation, remaining for kinematics). Any deviation in indexing or joint ordering breaks this assumption silently, as the network continues to optimize loss while ignoring distal joints.

\textbf{Solution:} The bug was fixed by explicitly specifying the slice end index, as shown below:
\begin{verbatim}
kin_input = joints[:,2:4]
\end{verbatim}

Additionally, we added assertions to verify gradient flow through all joint inputs:
\begin{verbatim}
assert kin_input.requires_grad
assert kin_input.grad_fn is not None
\end{verbatim}

We also implemented a unit test that verifies prediction invariance under joint perturbations:
{\footnotesize
\begin{verbatim}
def test_kinematic_sensitivity(model, base_angles):
    # Vary only distal joints, keep base fixed
    pred_base = model(base_angles)
    for delta in [-10, -5, 5, 10]:
        perturbed = base_angles.copy()
        perturbed[2:] += delta
        pred_perturbed = model(perturbed)
        assert np.mean((pred_base - pred_perturbed)**2) > 1e-4
\end{verbatim}
}
This test now runs after every training epoch, catching regression immediately.

\subsubsection{Challenge 3: Weighted Loss Tuning}
\label{subsubsec:challenge_3_weighted_loss_tuning}

Even after curriculum learning prevented catastrophic early mode collapse, standard MSE loss still drove convergence toward uniform black predictions because the underlying pixel class imbalance remained severe at full image resolution (85\% background vs. 15\% robot).

\textbf{Systematic Weight Exploration:}
We conducted a systematic empirical exploration of loss weighting strategies, varying the weight factor $w$ from 1 to 20. For each weight, we trained three models with different random seeds to account for stochasticity.

\begin{table}[H]
\centering
\caption{Effect of robot pixel weight on FFKSM performance.}
\label{tab:weight_tuning}
\begin{tabular}{ccccc}
\toprule
Weight $w$ & Train Loss & Valid Loss & PSNR (dB) & Convergence \\
\midrule
1 (unweighted) & 0.0182 & 0.0315 & 16.12 & Unstable \\
2 & 0.0154 & 0.0278 & 16.89 & Marginal \\
3 & 0.0123 & 0.0256 & 17.10 & Slow \\
4 & 0.0101 & 0.0241 & 17.22 & Good \\
\textbf{5} & \textbf{0.0092} & \textbf{0.0232} & \textbf{17.35} & \textbf{Stable} \\
6 & 0.0089 & 0.0245 & 17.18 & Oscillating \\
7 & 0.0085 & 0.0258 & 17.01 & Unstable \\
8 & 0.0082 & 0.0272 & 16.85 & Divergent \\
10 & 0.0078 & 0.0301 & 16.42 & Mode collapse \\
20 & 0.0071 & 0.0385 & 15.21 & Divergent \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Observations Narrative:} The empirical tuning sweeps mapped in Table~\ref{tab:weight_tuning} clarify the performance envelopes under different foreground penalty values. When evaluated at the unweighted metric base ($w=1$), the pipeline fails to isolate link boundaries, yielding an unstable validation PSNR of 16.12~dB. Incremental optimization to $w=2$ and $w=3$ marginally improves tracking response but restricts spatial sharpness to 16.89~dB and 17.10~dB respectively due to sluggish boundary adaptation. Setting $w=4$ provides clear visual definitions across the links, elevating the metric smoothly to 17.22~dB. The absolute performance ceiling is achieved at $w=5$, establishing an optimal stable validation anchor of 17.35~dB PSNR. Beyond this value ($w \ge 6$), the gradients become volatile, resulting in severe boundary artifacts and driving validation loss up to $0.0245$. Pushing past $w=10$ precipitates total structural mode collapse, resulting in completely uniform white frames.

\textbf{Analysis:} The optimal weight $w = 5$ emerges as the unique point that balances two competing pressures: 
1. \textbf{Sufficient signal strength}: The weight must be large enough that the sparse robot pixel gradients can overcome the overwhelming background gradient.
2. \textbf{Numerical stability}: The weight must be small enough that the loss landscape remains smooth and gradient descent remains stable. This precise sweet spot demonstrates that effective self-model training requires careful hyperparameter tuning beyond what is reported in original publications.

\subsection{FFKSM Training Protocol}
\label{subsec:ffksm_training_protocol}

\subsubsection{Hyperparameters}

\begin{table}[H]
\centering
\caption{FFKSM training hyperparameters.}
\label{tab:ffksm_hyperparams}
\begin{tabular}{lc}
\toprule
Parameter & Value \\
\midrule
Number of iterations & 8,000 \\
Learning rate & $5 \times 10^{-4}$ \\
Optimizer & Adam ($\beta_1=0.9$, $\beta_2=0.999$) \\
Samples per ray & 64 \\
Chunk size & 32,768 points \\
Pixel weight (robot) & 5.0 \\
Pixel weight (background) & 1.0 \\
Curriculum crop size & $50 \times 50$ \\
Curriculum duration & 500 iterations \\
Early stopping patience & 200 validation checks \\
Learning rate scheduler & ReduceLROnPlateau (factor=0.1, patience=20) \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{Training Loop Narrative Execution Steps:} The execution optimization sequence runs systematically across 8,000 total iterations. The loop starts by drawing a random training silhouette $I_{\text{GT}}$ paired with its ground-truth coordinate configuration $\mathbf{q}$. If the iteration lifespan tracks within the initialization bounds ($t < 500$), a cropping mask truncates both the visual canvas and the associated camera ray geometries to the central $50 \times 50$ envelope. Next, the pipeline synthesizes the ray distribution arrays, allocating $N_{\text{rays}} = 2,500$ paths during the early curriculum bounds and scaling up to 10,000 rays once the masking envelope expands to the full $100 \times 100$ boundary footprint.

Following ray layout configuration, the numerical integration module runs stratified sampling to draw $N=64$ discrete spatial density steps along each projected line. The coordinate and kinematic parameter blocks then feed these transformed space locations directly into the split MLP architecture to map the density vectors $\sigma$ and associated ray visibility coefficients $v$. Once computed, the volume rendering integration script aggregates these values to render the low-resolution visual reconstruction estimate $\hat{I}$. The optimization script evaluates the objective function by evaluating the weighted mean squared error formula:
\begin{equation}
\mathcal{L} = \frac{1}{N_{\text{pixels}}} \sum_{i,j} w(I_{ij}^{\text{GT}}) (\hat{I}_{ij} - I_{ij}^{\text{GT}})^2
\end{equation}
Finally, the backpropagation engine evaluates the parameters through automatic differentiation, invoking the Adam step updates to adjust the weights, and records a full validation tracking checkpoint every 2,000 iterations.

\subsection{FFKSM Performance Metrics}
\label{subsec:ffksm_performance_metrics}

After 8,000 iterations (approximately 50 minutes on NVIDIA Tesla T4), FFKSM achieved the performance outlined in Table~\ref{tab:ffksm_performance}.

\begin{table}[H]
\centering
\caption{FFKSM quantitative performance metrics.}
\label{tab:ffksm_performance}
\begin{tabular}{ll}
\toprule
\textbf{Metric} & \textbf{Value} \\
\midrule
PSNR (validation) & 17.35 dB \\
FPS (inference) & 5.22 FPS \\
Latency per frame & 191.6 ms \\
Number of parameters & 93,922 \\
Training time & 50.0 minutes \\
\bottomrule
\end{tabular}
\end{table}

The primary bottleneck remains volumetric rendering: despite efficient chunking (processing 32,768 points per batch to maximize GPU utilization), each forward pass requires approximately 33~ms due to the serial dependency of ray-marching. This latency---191.6~ms per frame---precludes real-time control integration at the 1~kHz rates common in robotic systems. Compared with the original FFKSM report (typically above 23~dB PSNR), our reproduction at 17.35~dB is lower because this thesis intentionally uses a smaller 2,000-sample dataset (1,600 train / 400 validation) rather than the 12,000-sample regime used in the original setup. This controlled reduction isolates latency behavior under limited-data conditions while preserving reproducibility across all baselines.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{ffksm_results.png}
    \caption{FFKSM validation targets vs. neural radiance field implicit model rendering outcomes. Left columns display train loss and right columns map validation curves, showing blurred link bounds and reconstruction artifacts along joint limits.}
    \label{fig:ffksm_results_baseline}
\end{figure}

\section{Kinematic 3D Gaussian Splatting (K-3DGS): Explicit Geometric Primitives}

To address FFKSM's speed limitation, we developed Kinematic 3D Gaussian Splatting (K-3DGS), which attaches 3D Gaussian primitives to a differentiable kinematic chain. This approach leverages GPU rasterization pipelines---optimized over decades of computer graphics research---to achieve faster rendering than ray-marching.

\subsection{Mathematical Foundations of 3D Gaussian Splatting}

3D Gaussian Splatting (3DGS) represents a scene as a set of $N$ discrete 3D Gaussian primitives. Each individual Gaussian is mathematically defined by a 3D mean position $\boldsymbol{\mu} \in \mathbb{R}^3$ and an anisotropic covariance matrix $\boldsymbol{\Sigma} \in \mathbb{R}^{3 \times 3}$. To ensure the covariance matrix remains positive semi-definite during optimization, it is factorized using scale and rotation constraints:
\begin{equation}
\boldsymbol{\Sigma} = \mathbf{R} \mathbf{S} \mathbf{S}^\mathsf{T} \mathbf{R}^\mathsf{T}
\end{equation}
where $\mathbf{S} = \text{diag}(\mathbf{s})$ is a diagonal scaling matrix and $\mathbf{R} \in SO(3)$ is a rotation matrix represented as a quaternion.

\subsection{Rendering with 3D Gaussian Splatting}

To render an image, 3DGS projects the 3D Gaussians onto the 2D image plane. For a given camera with viewing transformation $\mathbf{W}$ (world-to-camera) and projection $\mathbf{J}$ (Jacobian of the perspective projection), the projected 2D covariance $\boldsymbol{\Sigma}'$ is:
\begin{equation}
\boldsymbol{\Sigma}' = \mathbf{J} \mathbf{W} \boldsymbol{\Sigma} \mathbf{W}^\mathsf{T} \mathbf{J}^\mathsf{T}
\end{equation}

The 2D Gaussian function for pixel coordinates $\mathbf{u} \in \mathbb{R}^2$ is $G^{2D}(\mathbf{u}) = \exp\left(-\frac{1}{2} (\mathbf{u} - \boldsymbol{\mu}')^\mathsf{T} \boldsymbol{\Sigma}'^{-1} (\mathbf{u} - \boldsymbol{\mu}')\right)$, and the final pixel color is computed via alpha compositing with Gaussians sorted by depth:
\begin{equation}
C(\mathbf{u}) = \sum_{i=1}^{N} \alpha_i G_i^{2D}(\mathbf{u}) \mathbf{c}_i \prod_{j=1}^{i-1} (1 - \alpha_j G_j^{2D}(\mathbf{u}))
\end{equation}

\subsection{K-3DGS Architecture}

K-3DGS extends 3DGS to articulated robots by attaching Gaussian primitives to a kinematic chain. The architecture comprises four components:

\subsubsection{Component 1: Differentiable Forward Kinematics}

Forward kinematics is implemented in pure PyTorch for GPU acceleration and automatic differentiation. For each link $i$, the transformation matrix $\mathbf{T}_i \in SE(3)$ is computed using the Denavit-Hartenberg parameters:
\begin{equation}
\mathbf{T}_i = \mathbf{T}_{i-1} \mathbf{T}_{i-1}^{i}(\theta_i)
\end{equation}
where $\mathbf{T}_{i-1}^{i}(\theta_i)$ is the link transformation as a function of joint angle $\theta_i$. All transformations are differentiable with respect to joint angles, enabling end-to-end optimization.

\subsubsection{Component 2: Gaussian Initialization via Skeleton Placement}

Rather than random initialization (which leads to poor convergence), we initialize Gaussians along the robot's kinematic skeleton. The 600 Gaussians are distributed as:
\begin{itemize}
    \item \textbf{Base (Link 0)}: 100 Gaussians with radial distribution ($r = 0.05$ m) around the base origin
    \item \textbf{Link 1 (vertical segment)}: 200 Gaussians along the Z-axis from $z=0.02$ to $z=0.38$ m
    \item \textbf{Link 2 (horizontal segment)}: 200 Gaussians along the X-axis from $x=0.02$ to $x=0.38$ m
    \item \textbf{Link 3 (wrist)}: 100 Gaussians near the end-effector ($x \in [0.01, 0.09]$ m)
\end{itemize}

For each link, Gaussians are placed with small radial offsets to model the link thickness:
\begin{equation}
\mathbf{p}_{\text{local}} = \begin{bmatrix} x \\ r \cos\phi \\ r \sin\phi \end{bmatrix}
\end{equation}
where $x$ varies along the link axis, $r = 0.01$ m is the radius, and $\phi \in [0, 2\pi)$ is sampled uniformly. Each Gaussian maps localized parameters including position, scale, opacity, and color.

\subsubsection{Component 3: World Transformation}

The world position of each Gaussian is computed by applying the appropriate link transformation:
\begin{equation}
\boldsymbol{\mu}_j^{\text{world}} = \mathbf{T}_{\text{link}(j)} \begin{bmatrix} \boldsymbol{\mu}_j^{\text{local}} \\ 1 \end{bmatrix}
\end{equation}
where $\mathbf{T}_{\text{link}(j)}$ is the transformation matrix for the link to which Gaussian $j$ is attached.

\subsubsection{Component 4: Isotropic Rasterization}

We project Gaussians to 2D screen space using the perspective camera model (position $[1.0, 0.0, 0.0]$, focal length 130.2545). For isotropic Gaussians (spheres), the 2D projection simplifies to:
\begin{equation}
\text{radius}_{2D} = \frac{s \cdot f}{z}
\end{equation}
where $z$ is the depth of the Gaussian center in camera coordinates and $f$ is the focal length.

\subsection{The Anisotropic Failure: A Critical Architectural Lesson}

Our initial implementation attempted full anisotropic 3DGS with ellipsoidal Gaussians, following Kerbl et al.'s formulation. This principled approach---theoretically capable of representing thin, elongated robot links through appropriate ellipsoid orientations---failed catastrophically during training.

\subsubsection{Symptom}

After approximately 500 iterations, the model experienced mode collapse: all joint configurations produced identical images regardless of input. The loss stopped decreasing, and qualitative inspection revealed that all Gaussians had migrated to a single degenerate configuration near the robot base, with rotation matrices converging to similar orientations.

\subsubsection{Quantitative Analysis of the Failure}

We tracked the condition number of the covariance matrices throughout training. The condition number---the ratio of largest to smallest singular value of the covariance matrix---indicates how elongated the Gaussian is. Condition numbers above 10 indicate extreme anisotropy. By iteration 500, many Gaussians had become highly elongated (aspect ratios >10:1), but this elongation was not aligned with the robot link geometry, resulting in overlapping artifacts rather than faithful representation.

\subsubsection{Root Cause Analysis}

Gradient analysis revealed that optimizing rotation matrices in the non-convex $SO(3)$ manifold led to gradient explosion when combined with our limited 2,000-sample dataset. The high-dimensional parameter space exacerbated the problem: each anisotropic Gaussian requires 9 parameters, while each isotropic variant maps 5 variables.

The fundamental issue is the $SO(3)$ manifold's non-convexity. The rotation space has multiple local minima, and the gradient with respect to rotation parameters is proportional to the amount of "misalignment" between the Gaussian's orientation and the data. For featureless cylindrical links, there is no unique optimal orientation---any rotation about the cylinder's axis yields identical projections. This creates a flat valley in the loss landscape, causing the optimizer to wander aimlessly before eventually diverging.

\subsubsection{Attempted Remediations}

We attempted extensive remediation strategies, all of which failed to produce stable anisotropic training: learning rate shifts from $10^{-5}$ to $10^{-2}$, quaternion normalization maps, axis-angle steps, Euler setups, PCA skeleton initialization alignments, and structural determinant constraints. None resolved the instability.

\subsubsection{Forced Retreat to Isotropic Representations}

The anisotropic failure forced a pragmatic retreat to isotropic Gaussians. While theoretically less expressive (spheres cannot accurately represent cylindrical links), the isotropic formulation proved tractable and reliable. Training converges in 1,000 iterations (30.3 seconds) with learning rate $10^{-3}$ and no special stabilization.

\subsection{K-3DGS Performance Metrics}

The resulting K-3DGS model performance mirrors the parameters verified in Table~\ref{tab:k3dgs_performance}. Inference speedup over FFKSM is significant but still falls short of the 1 kHz control loop requirement.

\begin{table}[H]
\centering
\caption{K-3DGS quantitative performance metrics.}
\label{tab:k3dgs_performance}
\begin{tabular}{ll}
\toprule
\textbf{Metric} & \textbf{Value} \\
\midrule
PSNR (validation) & 17.01 dB \\
FPS (inference) & 37 FPS \\
Latency per frame & 27.0 ms \\
Number of Gaussians & 600 \\
Training time & 30.3 seconds \\
\bottomrule
\end{tabular}
\end{table}

More critically, the quality degraded slightly due to isotropic limitations. Qualitative inspection reveals characteristic "blobby" artifacts where thin links appear as chains of overlapping spheres rather than smooth cylinders. This validates our hypothesis that isotropic primitives sacrifice expressiveness for trainability.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{k3dgs_results.png}
    \caption{K-3DGS explicit geometric rendering validation: 2x3 validation image grid contrasting true robot arm poses against 3D Gaussian Splatting neural reconstructions, showing isotropic "blobby" aliasing artifacts along link boundaries.}
    \label{fig:k3dgs_results_baseline}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{3dgs_loss_speed_fps.png}
    \caption{K-3DGS training metrics showing loss convergence (top), training speed in FPS (middle), and step time in ms (bottom) across 1000 testing iterations.}
    \label{fig:k3dgs_metrics_baseline}
\end{figure}

\section{Comparative Analysis of Baseline Limitations}

\subsection{The Latency-Quality Tradeoff}

Table~\ref{tab:baseline_comparison_ch3} summarizes the two baseline architectures' performance:

\begin{table}[H]
\centering
\caption{Baseline self-modeling architecture performance comparison.}
\label{tab:baseline_comparison_ch3}
\begin{tabular}{lccc}
\toprule
\textbf{Method} & \textbf{PSNR (dB)} & \textbf{FPS} & \textbf{Latency (ms)} \\
\midrule
FFKSM (NeRF-based) & 17.35 & 5.22 & 191.6 \\
K-3DGS (Isotropic) & 17.01 & 37.00 & 27.0 \\
\bottomrule
\end{tabular}
\end{table}

Both architectures exceed the 1.0 ms control-loop budget by factors of 27 to 190. The root cause is architectural: both maintain an explicit or implicit 3D volumetric representation that requires either serial ray-marching (FFKSM) or per-primitive rasterization with depth sorting (K-3DGS).

\subsection{Supervision Efficiency Analysis}

The supervision efficiency explains the speed difference. FFKSM's ray-marching evaluates the MLP 640,000 times to produce a $100 \times 100 = 10,000$ pixel image. Each evaluation contributes gradient information for only $10,000 / 640,000 = 0.0156$ pixels---extremely sparse supervision. K-3DGS improves efficiency by a factor of 1,067 through parallel rasterization, but still requires per-primitive computations.

\subsection{Architectural Bottlenecks Summary}

Both baselines share fundamental limitations that prevent real-time deployment, notably serial dependency chains, per-primitive overhead, 3D representational weight, and training sample inefficiencies. These limitations motivate a paradigm shift: direct sensorimotor decoding that bypasses explicit 3D reconstruction entirely. The following chapter introduces NeuroKin, which eliminates the 3D bottleneck.
\clearpage

% ==========================================
% CHAPTER 4: THE NEUROKIN DIRECT SENSORIMOTOR DECODER
% ==========================================
\chapter{The NeuroKin Direct Sensorimotor Decoder}
\label{chap:neurokin_decoder}

The latency paradox identified in Chapter 3 stems from a fundamental architectural assumption shared by both FFKSM and K-3DGS: that self-modeling requires explicit or implicit 3D reconstruction as an intermediate representation. FFKSM maintains an implicit volumetric field queried via ray-marching; K-3DGS maintains explicit Gaussian primitives rasterized to 2D. Both incur computational costs proportional to the complexity of the 3D representation rather than the simplicity of the 2D output. This chapter challenges that assumption by introducing \textit{NeuroKin}, a lightweight fully convolutional network that maps joint angles directly to visual silhouettes, completely bypassing volumetric reconstruction. We demonstrate that for control-oriented tasks requiring only silhouette-level awareness, direct decoding achieves superior reconstruction fidelity (21.88 dB PSNR) at sub-millisecond latency (0.135 ms), representing a $1,418\times$ speedup over implicit NeRF baselines with a $+4.53$ dB quality improvement.

\section{Core Hypothesis: Bypassing 3D Reconstruction}

The central hypothesis motivating NeuroKin is that \textit{explicit 3D volumetric reconstruction is not a prerequisite for control-oriented self-modeling}. This hypothesis rests on three observations about the nature of robotic control tasks.

\subsection{The Task-Relevant Information Hierarchy}

For a manipulator performing reaching, grasping, or collision avoidance, the information required from a self-model can be hierarchically structured:
\begin{enumerate}
    \item \textbf{Silhouette-level awareness}: Where are the robot's links in the image plane? Which pixels are occupied? This information suffices for collision detection with known obstacles in camera coordinates.
    \item \textbf{Depth/Disparity information}: Given two calibrated views, silhouette disparity yields 3D position through triangulation. This recovers metric Cartesian coordinates without volumetric reconstruction.
    \item \textbf{Voxel-level occupancy}: Full 3D reconstruction is rarely required for manipulation. Most control tasks can be accomplished with end-effector position estimates and link-wise occupancy bounds.
\end{enumerate}
Critically, the computational cost of maintaining full 3D occupancy is orders of magnitude higher than estimating silhouette + disparity, yet the marginal utility of voxel-level detail is negligible for typical pick-and-place or trajectory tracking tasks.

\subsection{The Supervision Efficiency Argument}

The efficiency gap between volumetric rendering and direct decoding can be formalized in terms of \textit{supervision density}. Let $P$ be the number of output pixels ($P = 10,000$ for $100 \times 100$ images). Let $E$ be the number of network evaluations required to produce those $P$ pixels. For FFKSM with ray-marching:
\begin{equation}
E_{\text{FFKSM}} = P \times N_{\text{samples}} = 10,000 \times 64 = 640,000
\end{equation}
Each evaluation produces a single density value contributing to exactly one ray's accumulation. The supervision density---pixels informed per evaluation---is:
\begin{equation}
\rho_{\text{FFKSM}} = \frac{P}{E_{\text{FFKSM}}} = \frac{10,000}{640,000} = 0.0156\ \text{pixels/eval}
\end{equation}

For NeuroKin with direct decoding:
\begin{equation}
E_{\text{NeuroKin}} = 1 \quad \text{(single forward pass)}
\end{equation}
\begin{equation}
\rho_{\text{NeuroKin}} = \frac{P}{1} = 10,000\ \text{pixels/eval}
\end{equation}

The supervision efficiency ratio is therefore:
\begin{equation}
\frac{\rho_{\text{NeuroKin}}}{\rho_{\text{FFKSM}}} = \frac{10,000}{0.0156} = 640,000\times
\end{equation}
This $640,000\times$ advantage in supervision density explains how NeuroKin can simultaneously achieve faster inference \textit{and} better quality: each gradient update benefits from six orders of magnitude more information than a volumetric rendering update.

\subsection{When is 3D Reconstruction Necessary?}

We acknowledge that certain tasks require full 3D geometry:
\begin{itemize}
    \item Grasping unknown objects with complex geometry
    \item Precision insertion tasks requiring sub-millimeter pose estimation
    \item Navigation through narrow apertures where link-wise occupancy is insufficient
\end{itemize}
For these tasks, volumetric reconstruction remains necessary. However, for the majority of robotic control scenarios---including the fault tolerance and sequential coordination tasks evaluated in this thesis---silhouette-level self-awareness suffices. NeuroKin is positioned not as a universal replacement for 3D reconstruction but as a specialized architecture for the common case where 2D information is sufficient.

\section{Architecture Design}

NeuroKin implements direct sensorimotor decoding through a fully convolutional architecture comprising four sequential stages. The design prioritizes three objectives: (1) minimal inference latency, (2) smooth latent representations for robustness to joint noise, and (3) architectural simplicity for embedded deployment.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{CNN_architecture.png}
    \caption{NeuroKin convolutional decoder network architecture. Processes concatenated 2-view silhouette images through successive convolutional and fully connected dense layers to predict end effector position and joint angles.}
    \label{fig:neurokin_architecture_ch4}
\end{figure}

\subsection{Stage 1: Joint Encoder}

The joint encoder maps the 4-dimensional joint angle vector $\mathbf{q} \in [-90^\circ, +90^\circ]^4$ to a 1024-dimensional latent embedding $\mathbf{z} \in \mathbb{R}^{1024}$.

\subsubsection{Architecture Details}
The fully-connected layer sequence is structured as:
\begin{equation}
\mathbf{h}_1 = \text{ReLU}(\mathbf{W}_1 \mathbf{q} + \mathbf{b}_1), \quad \mathbf{W}_1 \in \mathbb{R}^{128 \times 4}, \mathbf{b}_1 \in \mathbb{R}^{128}
\end{equation}
\begin{equation}
\mathbf{h}_2 = \text{ReLU}(\mathbf{W}_2 \mathbf{h}_1 + \mathbf{b}_2), \quad \mathbf{W}_2 \in \mathbb{R}^{256 \times 128}, \mathbf{b}_2 \in \mathbb{R}^{256}
\end{equation}
\begin{equation}
\mathbf{z} = \mathbf{W}_3 \mathbf{h}_2 + \mathbf{b}_3, \quad \mathbf{W}_3 \in \mathbb{R}^{1024 \times 256}, \mathbf{b}_3 \in \mathbb{R}^{1024}
\end{equation}
where ReLU activation is defined as $\text{ReLU}(x) = \max(0, x)$.

\subsubsection{Design Rationale}

The latent dimension of 1024 was chosen through systematic ablation. Lower dimensions (256, 512) lacked capacity to represent the full joint manifold, resulting in underfitting (PSNR $\leq$ 20.12 dB). Higher dimensions (2048) overfit the 1,600 training samples, showing negligible quality improvement ($+0.07$ dB) at double the parameter count. The three-layer architecture (4 $\rightarrow$ 128 $\rightarrow$ 256 $\rightarrow$ 1024) provides sufficient expressiveness while maintaining computational efficiency. The progressive increase in dimension allows the network to build hierarchical representations: low-level joint angles are first combined into torque-like features (128-d), then into configuration-space features (256-d), and finally into the full latent code (1024-d) that will be spatially expanded.

\subsection{Stage 2: Spatial Expansion}

The spatial expansion layer transforms the 1D latent vector $\mathbf{z} \in \mathbb{R}^{1024}$ into a 4D spatial feature map $\mathbf{F}_0 \in \mathbb{R}^{256 \times 4 \times 4}$.

\subsubsection{Transformation}
The spatial broadcast formulation follows:
\begin{equation}
\mathbf{f}_{\text{flat}} = \text{ReLU}(\mathbf{W}_4 \mathbf{z} + \mathbf{b}_4), \quad \mathbf{W}_4 \in \mathbb{R}^{4096 \times 1024}, \mathbf{b}_4 \in \mathbb{R}^{4096}
\end{equation}
\begin{equation}
\mathbf{F}_0 = \text{reshape}(\mathbf{f}_{\text{flat}}, (256, 4, 4))
\end{equation}
where $4096 = 256 \times 4 \times 4$ is the product of output channels and spatial dimensions.

\subsubsection{Design Rationale}

The spatial expansion serves as a ``broadcast'' operation that converts the configuration-space latent representation into a format suitable for convolutional processing. The $4 \times 4$ spatial footprint is the smallest dimension that preserves sufficient locality for upsampling while minimizing computational cost. Each of the 256 channels can specialize in detecting different spatial patterns (e.g., vertical links, horizontal links, joint angles). The choice of $4 \times 4$ rather than $1 \times 1$ or $8 \times 8$ was empirically determined: $1 \times 1$ lacked spatial structure, causing the decoder to produce blurry outputs; $8 \times 8$ increased parameter count by $4\times$ without quality improvement.

\subsection{Stage 3: Transposed Convolutional Decoder}

The decoder progressively upsamples the spatial feature map through four transposed convolution stages, each doubling spatial resolution while halving channel count.

\subsubsection{Layer Definitions and Tensor Transformations}

To mathematically trace the dimensional mapping, let $\mathcal{T}_k(\mathbf{X}; \mathbf{W}, \mathbf{b})$ define a transposed convolutional operator with a kernel spatial footprint $k \times k$, stride $s=2$, and padding $p=1$. Stage 3 maps the high-dimensional latent structures across expanding spatial grids via the following sequence of transformations:
\begin{align}
\mathbf{F}_1 &= \text{ReLU}\left(\text{BN}\left(\mathcal{T}_{4\times4}(\mathbf{F}_0; \mathbf{W}_{T1}, \mathbf{b}_{T1})\right)\right), \quad \mathbf{F}_1 \in \mathbb{R}^{128 \times 8 \times 8} \\
\mathbf{F}_2 &= \text{ReLU}\left(\text{BN}\left(\mathcal{T}_{4\times4}(\mathbf{F}_1; \mathbf{W}_{T2}, \mathbf{b}_{T2})\right)\right), \quad \mathbf{F}_2 \in \mathbb{R}^{64 \times 16 \times 16} \\
\mathbf{F}_3 &= \text{ReLU}\left(\text{BN}\left(\mathcal{T}_{4\times4}(\mathbf{F}_2; \mathbf{W}_{T3}, \mathbf{b}_{T3})\right)\right), \quad \mathbf{F}_3 \in \mathbb{R}^{32 \times 32 \times 32} \\
\mathbf{F}_4 &= \text{ReLU}\left(\text{BN}\left(\mathcal{T}_{4\times4}(\mathbf{F}_3; \mathbf{W}_{T4}, \mathbf{b}_{T4})\right)\right), \quad \mathbf{F}_4 \in \mathbb{R}^{16 \times 64 \times 64}
\end{align}
where BN denotes batch normalization.

\subsubsection{Transposed Convolution Mathematics}

For a standard convolution, the output size $o$ given input size $i$, kernel $k$, stride $s$, and padding $p$ is:
\begin{equation}
o = \left\lfloor \frac{i + 2p - k}{s} \right\rfloor + 1
\end{equation}
For transposed convolution, the output size scales inversely according to:
\begin{equation}
o = (i - 1)s + k - 2p
\end{equation}
With $i=4$, $k=4$, $s=2$, $p=1$:
\begin{equation}
o = (4 - 1)\cdot 2 + 4 - 2\cdot 1 = 3\cdot 2 + 4 - 2 = 6 + 2 = 8
\end{equation}
Each transposed convolution thus exactly doubles the spatial dimension ($4 \rightarrow 8 \rightarrow 16 \rightarrow 32 \rightarrow 64$).

\subsubsection{Batch Normalization}

Batch normalization is applied after each transposed convolution before the ReLU activation:
\begin{equation}
\text{BN}(x) = \gamma \frac{x - \mu_{\mathcal{B}}}{\sqrt{\sigma_{\mathcal{B}}^2 + \epsilon}} + \beta
\end{equation}
where $\mu_{\mathcal{B}}$ and $\sigma_{\mathcal{B}}^2$ are the batch mean and variance, $\gamma$ and $\beta$ are learnable affine parameters, and $\epsilon = 10^{-5}$ for numerical stability. BN reduces internal covariate shift, allowing higher learning rates and faster convergence.

\subsubsection{Design Rationale for Four Layers}

The number of transposed convolution layers (four) was chosen to match the factor between initial spatial size ($4 \times 4$) and final output size ($64 \times 64$), requiring $\log_2(64/4) = 4$ doublings. The final $64 \times 64$ is then upsampled to $100 \times 100$ via bilinear interpolation. Ablation studies show that fewer layers (two or three) produce insufficient resolution for fine link boundaries; more layers (five or six) increase latency without quality improvement due to over-parameterization.

\subsection{Stage 4: Output Head and Spatial Upsampling}

The output head converts the 16-channel $64 \times 64$ feature map to a single-channel silhouette at $100 \times 100$ resolution.

\subsubsection{Architecture}
The sequential output mapping follows:
\begin{equation}
\mathbf{F}_5 = \text{ReLU}(\text{Conv2d}_{3\times3}(\mathbf{F}_4)), \quad \text{Conv2d}_{3\times3}: 16 \rightarrow 8
\end{equation}
\begin{equation}
\mathbf{F}_6 = \text{Conv2d}_{1\times1}(\mathbf{F}_5), \quad \text{Conv2d}_{1\times1}: 8 \rightarrow 1
\end{equation}
\begin{equation}
\hat{I}_{\text{low}} = \sigma(\mathbf{F}_6) \in [0, 1]^{64 \times 64}
\end{equation}
\begin{equation}
\hat{I} = \text{bilinear\_upsample}(\hat{I}_{\text{low}}, (100, 100))
\end{equation}
where $\sigma(x) = 1 / (1 + \exp(-x))$ is the sigmoid activation mapping logits to probabilities.

\subsubsection{Design Rationale}

The output head separates feature extraction from mask prediction. The $3 \times 3$ convolution blends features across channels, smoothing local noise. The $1 \times 1$ convolution projects the smoothed features to a single dimension, generating logits. Sigmoid activation bounds the outputs to $[0, 1]$, representing pixel-wise occupancy probability. Bilinear upsampling to $100 \times 100$ avoids the checkerboard artifacts associated with non-integer transposed convolutions while preserving sub-pixel boundary locations.

\section{Training Optimization Loop and Loss Profiles}

NeuroKin is optimized end-to-end using Binary Cross-Entropy loss on an explicit batch configuration.

\subsection{Objective Function}

The objective function combines standard BCE loss with a foreground weight factor to manage the class imbalance described in Chapter 3:
\begin{equation}
\mathcal{L}_{\text{BCE}} = -\frac{1}{P} \sum_{i=1}^{P} \left[ w \cdot I_i \log \hat{I}_i + (1 - I_i) \log(1 - \hat{I}_i) \right]
\end{equation}
where $P = 10,000$ is the total pixel count, $I_i \in \{0, 1\}$ is the ground-truth pixel value, $\hat{I}_i \in [0, 1]$ is the predicted probability, and $w = 5.0$ is the foreground weight multiplier optimized in Chapter 3.

\subsection{Optimization Parameters}

\begin{itemize}
    \item \textbf{Batch Size}: 32 samples per batch. This value maximizes memory bus saturation on the Tesla T4 GPU while maintaining stable gradient variance.
    \item \textbf{Learning Rate}: Initial learning rate $\eta = 10^{-3}$ with Adam optimizer ($\beta_1 = 0.9, \beta_2 = 0.999$).
    \item \textbf{Scheduler}: Cosine annealing scheduler decays learning rate to $10^{-5}$ over 100 epochs.
    \item \textbf{Regularization}: Weight decay $\lambda = 10^{-4}$ on all convolutional kernels prevents overfitting.
\end{itemize}

\subsection{Convergence Timeline and Metrics}

Training proceeds rapidly compared to volumetric methods. Figure~\ref{fig:neurokin_training_metrics} details loss attenuation and peak signal-to-noise ratio (PSNR) trends during training.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{neurokin_2d_loss_training.png}
    \caption{NeuroKin training convergence and fidelity tracking profiles. Left plot charts Binary Cross-Entropy training loss vs. iterations; right plot tracks validation performance in PSNR vs. iterations.}
    \label{fig:neurokin_training_metrics}
\end{figure}

The BCE loss decreases monotonically from 0.69 to 0.045 within 2,000 iterations (approx. 20 seconds), indicating stable gradient propagation. Validation PSNR rises correspondingly, stabilizing at 21.88 dB after 3,000 iterations. Total training time is 33.4 seconds across 50 full epochs, representing a $90\times$ acceleration over FFKSM training (50 minutes) due to the elimination of ray-marching sampling loops.

\section{Quantitative Evaluation and Ablation Studies}

We conduct rigorous benchmarking and ablation studies to isolate the factors contributing to NeuroKin's speed and quality.

\subsection{Ablation 1: Latent Dimension of the Joint Encoder}

We vary the hidden dimension of the final fully connected layer in Stage 1, training all models for 50 epochs with identical seeds.

\begin{table}[H]
\centering
\caption{Ablation study on joint encoder latent dimension.}
\label{tab:ablation_latent}
\begin{tabular}{ccccc}
\toprule
\textbf{Latent Dimension} & \textbf{Parameters} & \textbf{Valid Loss} & \textbf{PSNR (dB)} & \textbf{Latency (ms)} \\
\midrule
128 & 124,545 & 0.089 & 18.42 & 0.112 \\
256 & 256,129 & 0.064 & 19.85 & 0.118 \\
512 & 519,297 & 0.051 & 20.12 & 0.124 \\
\textbf{1024} & \textbf{1,046,145} & \textbf{0.038} & \textbf{21.88} & \textbf{0.135} \\
2048 & 2,101,121 & 0.037 & 21.95 & 0.168 \\
\bottomrule
\end{tabular}
\end{table}

The dimension 1024 represents the optimal balance. Moving to 2048 increases parameter count by $100\%$ but yields a negligible $+0.07$ dB increase in PSNR while introducing a $+24\%$ latency penalty due to increased memory transfer overhead.

\subsection{Ablation 2: Number of Transposed Convolution Layers}

We test variants with 3, 4, and 5 upsampling layers, adjusting the spatial expansion layer to maintain the final $100 \times 100$ resolution.

\begin{table}[H]
\centering
\caption{Ablation study on transposed convolution layer count.}
\label{tab:ablation_layers}
\begin{tabular}{ccccc}
\toprule
\textbf{Layers} & \textbf{Initial Shape} & \textbf{Final Output Head Shape} & \textbf{PSNR (dB)} & \textbf{Latency (ms)} \\
\midrule
3 & $8 \times 8$ & $64 \times 64$ (Bilinear to 100) & 19.45 & 0.110 \\
\textbf{4} & $\mathbf{4 \times 4}$ & $\mathbf{64 \times 64}$ \textbf{(Bilinear to 100)} & \textbf{21.88} & \textbf{0.135} \\
5 & $2 \times 2$ & $64 \times 64$ (Bilinear to 100) & 21.91 & 0.182 \\
\bottomrule
\end{tabular}
\end{table}

The 4-layer design maximizes quality. The 3-layer design processes an initial $8 \times 8$ map, which lacks sufficient abstract spatial resolution, causing fuzzy boundaries and dropping PSNR by 2.43 dB. The 5-layer design increases latency by 34\% without structural benefit.

\subsection{Ablation 3: Activation Functions}

We substitute ReLU with LeakyReLU ($\alpha = 0.01$) and GELU across all internal layers.

\begin{table}[H]
\centering
\caption{Ablation study on activation functions.}
\label{tab:ablation_activation}
\begin{tabular}{lccc}
\toprule
\textbf{Activation} & \textbf{Valid Loss} & \textbf{PSNR (dB)} & \textbf{Latency (ms)} \\
\midrule
\textbf{ReLU} & \textbf{0.038} & \textbf{21.88} & \textbf{0.135} \\
LeakyReLU & 0.039 & 21.74 & 0.141 \\
GELU & 0.038 & 21.82 & 0.154 \\
\bottomrule
\end{tabular}
\end{table}

ReLU yields the minimum inference latency. GELU requires evaluating error functions (erf), which increases mathematical operations per neuron and drives latency up by 14\%, confirming that standard ReLU is optimal for low-latency hardware loops.

\section{The Architectural Comparison: Resolving the Latency Paradox}

Table~\ref{tab:final_architectural_comparison} directly contrasts NeuroKin against the baseline models analyzed in Chapter 3.

\begin{table}[H]
\centering
\caption{Final comprehensive architectural performance summary.}
\label{tab:final_architectural_comparison}
\begin{tabular}{lcccc}
\toprule
\textbf{Method} & \textbf{PSNR (dB)} & \textbf{FPS} & \textbf{Latency (ms)} & \textbf{Training Time} \\
\midrule
FFKSM (Implicit NeRF) & 17.35 & 5.22 & 191.600 & 50.0 minutes \\
K-3DGS (Explicit Points) & 17.01 & 37.00 & 27.000 & 30.3 seconds \\
\textbf{NeuroKin (Proposed)} & \textbf{21.88} & \textbf{7,407} & \textbf{0.135} & \textbf{33.4 seconds} \\
\bottomrule
\end{tabular}
\end{table}

NeuroKin satisfies the sub-millisecond constraint required for closed-loop execution. By mapping variables directly through a convolutional network, it achieves an inference latency of 0.135 ms (7,407 FPS), representing a $1,418\times$ speedup over implicit FFKSM and a $200\times$ speedup over explicit K-3DGS. Simultaneously, it increases quality by $+4.53$ dB over FFKSM.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{neurokin_results.png}
    \caption{NeuroKin direct sensorimotor decoding visualization: 2x3 grid mapping target/ground truth images alongside synthetic NeuroKin renderings and corresponding pixel-wise discrepancy difference maps, showing sharp boundary reconstruction.}
    \label{fig:neurokin_visual_results}
\end{figure}

Qualitative inspection (Figure~\ref{fig:neurokin_visual_results}) explains this performance: NeuroKin reproduces sharp link boundaries and fine joint extremities without the blur of FFKSM or the blobby artifacts of K-3DGS. This supports the core thesis hypothesis: explicit 3D volumetric representations are structurally redundant for silhouette-level visual self-modeling, and direct convolutional decoding provides a more efficient mechanism for real-time robotic perception.

\section{ResNeuroKin-D: Multi-Task Depth Extension}

To verify whether direct decoding can recover 3D metric data without explicit volumetric queries, we design an auxiliary multi-task variant named \textit{ResNeuroKin-D}.

\subsection{Architecture and Objective}

ResNeuroKin-D adds a parallel depth decoding head alongside the silhouette head. The network uses residual connections (ResNet-style blocks) in the decoder stages to handle the extra training gradients. The loss function is:
\begin{equation}
\mathcal{L}_{\text{multi}} = \mathcal{L}_{\text{BCE}} + \alpha \mathcal{L}_{\text{Depth\_MSE}}
\end{equation}
where $\mathcal{L}_{\text{Depth\_MSE}} = \frac{1}{P} \sum \|D_i - \hat{D}_i\|^2$ measures the mean squared error between ground-truth simulation depth maps and predicted depth values across occupied pixels, with scaling factor $\alpha = 1.0$.

\subsection{Fidelity and Speed Tradeoffs}

ResNeuroKin-D maintains sub-millisecond execution bounds, achieving a validation silhouette PSNR of 21.12 dB and a root-mean-squared depth error of 4.2 mm across the workspace. Inference latency rises to 0.400 ms (2,500 FPS) due to the depth head's extra convolution layers and parameters. This variant remains compatible with 1 kHz control deadlines, demonstrating that direct decoding can scale to multi-task spatial recovery without reverting to volumetric ray-marching routines.

\section{Stereoscopic Configuration for 3D Coordinate Recovery}

To extract metric 3D Cartesian coordinates for the inverse-kinematics control loop, we implement a stereoscopic configuration using two synchronized NeuroKin networks.

\subsection{Dual-Camera Deployment Setup}

Two identical virtual cameras are mounted in the PyBullet simulator: Camera A at position $[1.0, 0.0, 0.0]$ and Camera B at position $[0.0, 1.0, 0.0]$, both looking at the origin. Two independent NeuroKin networks are trained, one for each view, using the data generation pipeline described in Chapter 3.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{2_view_data_generation_sample.png}
    \caption{Stereoscopic configuration setup: orthogonal images of 2 views of the robot arm generated concurrently for training individual view networks.}
    \label{fig:two_view_generation}
\end{figure}

During inference, the current joint angle vector $\mathbf{q}$ is sent to both networks simultaneously, generating predicted silhouettes $\hat{I}_A$ and $\hat{I}_B$ in parallel batches.

\subsection{Triangulation Mechanics and Coordinate Extraction}

The 3D spatial position of the end-effector is extracted directly from the predicted 2D silhouettes without a volumetric model. The algorithm proceeds as follows:
\begin{enumerate}
    \item Find the end-effector centroid $(u_A, v_A)$ in image plane A and $(u_B, v_B)$ in image plane B by isolating the tip pixels of the predicted masks using a fixed kinematic template matching routine.
    \item Project the 2D centroids into 3D camera rays using the known camera projection matrices:
    \begin{equation}
    \mathbf{r}_A(t) = \mathbf{o}_A + t \cdot \mathbf{K}^{-1} \begin{bmatrix} u_A \\ v_A \\ 1 \end{bmatrix}, \quad \mathbf{r}_B(s) = \mathbf{o}_B + s \cdot \mathbf{K}^{-1} \begin{bmatrix} u_B \\ v_B \\ 1 \end{bmatrix}
    \end{equation}
    where $\mathbf{o}$ is the camera origin and $\mathbf{K}$ is the intrinsic calibration matrix.
    \item Compute the intersection of the two rays. Because of network noise, the rays may not intersect perfectly; we find the closest point of approach by minimizing the squared distance between ray coordinates:
    \begin{equation}
    \mathbf{x}_{\text{recovered}} = \arg\min_{\mathbf{x}} \left( \text{dist}(\mathbf{x}, \mathbf{r}_A)^2 + \text{dist}(\mathbf{x}, \mathbf{r}_B)^2 \right)
    \end{equation}
\end{enumerate}

The complete stereoscopic inference, centroid extraction, and triangulation pipeline executes in 0.285 ms on the Tesla T4 hardware, providing a 3D state update frequency of 3.5 kHz. Figure~\ref{fig:two_view_scatter} validates the precision of this process across 2,000 workspace points.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{2_view_end_effector_position.png}
    \caption{3D workspace validation scatter plot: 3D scatter plot detailing true vs. stereoscopically triangulated end-effector positions across 2000 generated train + test samples.}
    \label{fig:two_view_scatter}
\end{figure}

The mean spatial tracking error across the workspace is 1.84 mm. This level of error is precise enough for trajectory-following control, confirming that direct sensorimotor decoding can recover metric spatial data within tight control timelines.
\clearpage

% ==========================================
% CHAPTER 5: CLOSED-LOOP FAULT ADAPTATION
% ==========================================
\chapter{Closed-Loop Fault Adaptation}
\label{chap:closed_loop_fault_adaptation}

The static validation in Chapter 4 established NeuroKin's speed and spatial precision. This chapter evaluates its dynamic performance inside a feedback control system under structural damage conditions. We integrate NeuroKin's stereoscopic spatial updates into a closed-loop inverse kinematics control structure and test its capacity to stabilize a 4-DOF manipulator following severe mechanical fault injection.

\section{Control Loop Integration Mechanics}

The visual closed-loop control system combines classical differential kinematics with NeuroKin's high-speed perception pipeline. Figure~\ref{fig:control_loop_flowchart} maps the single-agent architectural layout.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{control_loop_flowchart.pdf}
    \caption{Algorithmic block flowchart mapping a single-agent inverse kinematics closed-loop system, tracking state initialization, neural network self-model evaluation, Jacobian update calculations, and adaptation.}
    \label{fig:control_loop_flowchart}
\end{figure}

\subsection{Jacobian-Based Inverse Kinematics}

The control task requires the robot's end-effector to track a Cartesian trajectory $\mathbf{x}_{\text{target}}(t) \in \mathbb{R}^3$. The tracking controller updates joint positions by evaluating the pseudo-inverse Jacobian matrix at each time step \cite{siciliano2009robotics}:
\begin{equation}
\Delta \mathbf{q} = \mathbf{J}^{\dagger}(\mathbf{q}) \cdot e_{\mathbf{x}}
\end{equation}
where $\mathbf{J}^{\dagger} = \mathbf{J}^\mathsf{T}(\mathbf{J}\mathbf{J}^\mathsf{T})^{-1}$ is the Moore-Penrose pseudo-inverse of the analytical manipulator Jacobian, and $e_{\mathbf{x}} = \mathbf{x}_{\text{target}} - \mathbf{x}_{\text{current}}$ represents the Cartesian error vector.

\subsection{The Feedback Source Switch}

Under nominal operating conditions, the current position is calculated via standard forward kinematics using joint encoder values: $\mathbf{x}_{\text{current}} = f_{\text{FK}}(\mathbf{q}_{\text{enc}})$. When a fault is detected or suspected, the feedback loop switches to the stereoscopic NeuroKin pipeline: $\mathbf{x}_{\text{current}} = \mathbf{x}_{\text{recovered}}$. The joint update law then becomes:
\begin{equation}
\mathbf{q}_{k+1} = \mathbf{q}_k + \eta \cdot \mathbf{J}^{\dagger}(\mathbf{q}_{\text{enc}}) \left( \mathbf{x}_{\text{target}} - \mathbf{x}_{\text{recovered}} \right)
\end{equation}
where $\eta = 0.1$ is the loop gain constraint. Crucially, the analytical Jacobian still reads from the proprioceptive encoders $\mathbf{q}_{\text{enc}}$, while the error vector reads from the visual self-model $\mathbf{x}_{\text{recovered}}$. This decoupling allows the loop to converge even when encoder readings are corrupted by damage.

\section{Fault Injection Methodology}

We inject two classes of mechanical faults into the PyBullet simulation at step $t = 50$:

\subsection{Fault 1: Severe Joint Encoder Slip}

Joint encoder slip represents an abrupt sensor calibration failure caused by mechanical slippage or electrical noise. At step 50, a constant bias vector is added to the joint encoder readings:
\begin{equation}
\mathbf{q}_{\text{enc}} = \mathbf{q}_{\text{true}} + \Delta \mathbf{q}_{\text{fault}}
\end{equation}
where $\Delta \mathbf{q}_{\text{fault}} = [0.35, -0.42, 0.51, 0.0]$ radians ($\approx 20^\circ$ to $30^\circ$ offset bounds). The controller receives these corrupted readings, causing its forward kinematics model to calculate incorrect end-effector locations.

\subsection{Fault 2: Structural Link Damage}

Structural link damage represents a physical geometric alteration caused by a collision or bending impact. At step 50, the physical length of Link 2 in the simulation URDF is reduced by 25\% (from 0.40 m to 0.30 m) to simulate structural compression, and a permanent $15^\circ$ bending offset is introduced at the link joint boundary. The controller remains unaware of this change, continuing to use nominal DH parameters to calculate pseudo-inverse updates.

\section{Dynamic Trajectory Tracking Results}

We evaluate tracking convergence by commanding the end-effector to follow a spatial trajectory over 200 control steps.

\subsection{Nominal Performance Analysis}

Under nominal conditions without fault injection, both the encoder feedback loop and the NeuroKin feedback loop track the target path accurately. Figure~\ref{fig:joint_error_nominal} logs joint error profiles during nominal operation.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{closedloop-joint-error.png}
    \caption{Nominal feedback convergence tracking: multi-line graph tracking simultaneous error convergence profiles in radians across 4 independent robotic joints over 200 tracking steps.}
    \label{fig:joint_error_nominal}
\end{figure}

Errors across all four joints decrease exponentially, reaching steady-state convergence within 40 steps. The visual tracking loop achieves a steady-state error margin of 1.9 mm, proving that NeuroKin's triangulated spatial estimates are sufficiently smooth and stable to drive classical feedback loops without inducing high-frequency chatter or divergence.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{closedloop-endeffector-position-plot.png}
    \caption{3D workspace trajectory tracking: workspace trajectory plot tracing the continuous spatial transit path of the end-effector position, comparing ground truth targets directly with prediction profiles.}
    \label{fig:trajectory_plot_nominal}
\end{figure}

Figure~\ref{fig:trajectory_plot_nominal} shows the spatial alignment between the target trajectory and the path recovered by the stereoscopic network, confirming operational accuracy in the absence of damage.

\subsection{Post-Fault Divergence of Proprioceptive Tracking}

When a fault is injected at step 50 without visual feedback, the tracking loop fails immediately. Figure~\ref{fig:error_comparison_fault} illustrates this failure.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{closedloop-endeffector-error-with-damage.png}
    \caption{Post-fault tracking error comparison: timeline graph charting end-effector tracking error across 200 sequential steps, comparing baseline, baseline with fault, and NeuroKin with fault configurations.}
    \label{fig:error_comparison_fault}
\end{figure}

The proprioceptive baseline controller (blue curve) experiences a rapid expansion in tracking error at step 50. Because the encoder readings are biased, the controller calculates that it is on target when it is physically distant. The end-effector drifts away from the target path, with the Euclidean tracking error rising from 2 mm to an unstable steady-state error of 142 mm, causing the tracking task to fail.

\subsection{NeuroKin-Driven Error Recovery}

When the control loop switches to NeuroKin visual feedback at step 50 (orange curve in Figure~\ref{fig:error_comparison_fault}), the system adapts to the damage. Following a brief transient spike caused by the sudden shift in feedback coordinates, the error vector converges back toward nominal parameters:
\begin{equation}
e_{\mathbf{x}} = \mathbf{x}_{\text{target}} - \mathbf{x}_{\text{recovered}} \rightarrow 0
\end{equation}

Because NeuroKin estimates position visually, it bypasses the corrupted encoder signals and structural DH parameters. The tracking error drops below 5.0 mm within 35 steps of fault injection, demonstrating that sub-millisecond visual self-modeling can actively stabilize a damaged manipulator.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{closedloop-endeffector-error.png}
    \caption{Visual feedback error attenuation: line graph charting the physical decay of Euclidean coordinate error distance for a robot end-effector over a 200-step closed-loop tracking operation.}
    \label{fig:error_decay_visual}
\end{figure}

Figure~\ref{fig:error_decay_visual} plots this error decay, confirming that the visual feedback loop restores tracking accuracy after severe structural damage.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{end-effector-position_gt_vs_prediction.png}
    \caption{Multi-line validation tracking graph overlaying ground-truth trajectory values directly against network coordinate predictions for the individual X, Y, and Z Cartesian axes across 100 index samples.}
    \label{fig:gt_vs_pred_axes}
\end{figure}

Figure~\ref{fig:gt_vs_pred_axes} overlays the true coordinate tracking paths directly against NeuroKin's coordinate assertions across the individual Cartesian axes, showing tight alignment throughout the tracking sequence.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{end-effector-position-and-joint-angles-error.png}
    \caption{Horizontal multi-panel plot display containing two diagnostic charts: left panel is a 3D plot overlaying ground-truth end effector position against the NeuroKin predicted position states; right panel tracks the joint angle mean absolute error per joint of NeuroKin prediction vs ground truth.}
    \label{fig:multipanel_diagnostic}
\end{figure}

Figure~\ref{fig:multipanel_diagnostic} provides a final multi-panel diagnostic summary, confirming that mean absolute errors remain low across all links and joints during closed-loop operation.
\clearpage

% ==========================================
% CHAPTER 6: SEQUENTIAL SWARM COORDINATION
% ==========================================
\chapter{Sequential Swarm Coordination}
\label{chap:sequential_swarm_coordination}

Chapter 5 demonstrated that NeuroKin can stabilize a single manipulator under severe fault conditions. This chapter scales the evaluation to a multi-agent system, investigating how individual high-speed self-diagnosis can support collective resilience across a decentralized robotic swarm. We evaluate coordination using a 100-stage sequential pipeline managed via temporal stigmergy mechanics.

\section{Algorithmic Structure of Temporal Stigmergy}

We implement multi-agent coordination using a digital memory ledger that abstracts the stigmergic communication observed in biological swarms \cite{bongard2006resilient}. Figure~\ref{fig:swarm_flowchart} maps the decentralized control architecture.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{swarm_control_loop_flowchart.pdf}
    \caption{Decentralized multi-agent control flowchart: block chart documenting a multi-agent decentralized closed-loop control system tracing environment state routing through individual agent policy networks to orchestrate collective swarm movements.}
    \label{fig:swarm_flowchart}
\end{figure}

The swarm comprises 100 independent robotic agents $\{A_1, A_2, \ldots, A_{100}\}$ that execute their tasks sequentially. The agents do not communicate directly; instead, they coordinate by reading and writing to a persistent global state structure named \texttt{factory\_state}.

\subsection{Ledger State Parameters}

The \texttt{factory\_state} dictionary stores three variables:
\begin{itemize}
    \item \texttt{consecutive\_failures}: An integer tracking the total number of consecutive agent failures in the pipeline.
    \item \texttt{system_fault_alert}: A boolean flag that triggers when systemic mechanical damage is identified.
    \item \texttt{calibration_offset}: A shared floating-point array that stores estimated baseline coordinate corrections.
\end{itemize}

\subsection{The Execution Protocol}

Each agent $A_i$ follows a four-step execution protocol:
\begin{enumerate}
    \item \textbf{Read}: At initialization, agent $A_i$ reads the current variables from the shared \texttt{factory\_state} ledger.
    \item \textbf{Adapt}: The agent modifies its local controller behavior based on the ledger history. If \texttt{consecutive\_failures} $\ge 2$, the agent activates a defensive "recovery mode," shifting its target from a randomized workspace coordinate to a conservative centralized position and doubling its Cartesian tracking tolerance boundary.
    \item \textbf{Execute}: The agent runs its closed-loop manipulation task over 100 steps. Task success requires the final tracking error to satisfy: $e_{\text{final}} \le \tau$, where $\tau = 5.0$ mm under nominal mode or $10.0$ mm under recovery mode.
    \item \textbf{Write}: Upon task completion, the agent logs its performance to the ledger. If successful, \texttt{consecutive\_failures} resets to 0. If it fails, the integer increments:
    \begin{equation}
    \texttt{consecutive\_failures}_{i} = \texttt{consecutive\_failures}_{i-1} + 1
    \end{equation}
\end{enumerate}

\section{Compounding Swarm Fault Scenario}

We test the swarm under an escalating fault scenario where mechanical degradation expands across successive agents. The base probability of fault injection scales linearly with the agent index:
\begin{equation}
P(\text{Fault} \mid A_i) = 0.008 \cdot i
\end{equation}
By stage 50, the fault injection probability reaches 40\%, and by stage 100, it scales to 80\%. The faults injected follow the encoder slip and link bending models described in Chapter 5. This progression simulates an industrial environment undergoing progressive mechanical wear or a planetary deployment facing environmental hazards.

\section{Swarm Evaluation Results}

We evaluate the coordination architecture by comparing the NeuroKin stigmergic pipeline against a traditional independent control baseline across a 100-stage simulation run.

\subsection{The Baseline Collapse}

The standard independent control pipeline collapses under escalating faults. Figure~\ref{fig:swarm_bar_plots} outlines the comparative results.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{Neurokin_vs_Basline_with_fault.png}
    \caption{Swarm coordination efficiency statistics: left panel compares overall success rates for baseline pre-fault, baseline post-fault, and NeuroKin post-fault configurations; right panel tracks the mean steps required to reach the target configuration.}
    \label{fig:swarm_bar_plots}
\end{figure}

During the first 20 stages, while fault injection is rare, the baseline maintains a 100\% success rate. However, as the fault probability increases past stage 40, the lack of inter-agent communication causes cascading failures. Damaged agents, relying exclusively on corrupted encoder data, miss their targets but report success to the system, leaving the underlying calibration faults undetected. The system post-fault success rate drops to 13\% between stages 50 and 100, causing overall operation to fail.

\subsection{NeuroKin-Enabled Stigmergic Endurance}

When agents are equipped with the stereoscopic NeuroKin self-model and integrated into the temporal ledger, the swarm maintains operational continuity. Individual agents use sub-millisecond inference to detect failures within a single control cycle. When an agent fails due to severe damage, it logs the event to the ledger. Subsequent agents read this trace, identify the systemic fault risk, and activate recovery mode. 

As shown in Figure~\ref{fig:swarm_bar_plots}, the NeuroKin stigmergic swarm preserves an 88% systemic success rate under identical fault injection profiles. When damage occurs, the temporal feedback loop coordinates adaptive behaviors across successive stages, reducing the mean steps to reach target constraints by 62% compared to the baseline. Proving that sub-millisecond self-diagnosis can drive temporal stigmergy validates the computational prerequisite for multi-agent swarm coordination under compounding mechanical wear.
\clearpage

% ==========================================
% CHAPTER 7: EMPIRICAL DISCUSSION
% ==========================================
\chapter{Empirical Discussion}
\label{chap:discussion_main}

This chapter evaluates the experimental results from Chapters 4, 5, and 6, analyzes the architectural trade-offs of the proposed direct decoding paradigm, and examines threats to validity under realistic hardware deployment constraints.

\section{Analysis of the Central Hypothesis}

The experimental data validate the central hypothesis of this thesis: \textit{explicit 3D volumetric reconstruction is not a prerequisite for control-oriented self-modeling}. NeuroKin's performance (21.88 dB PSNR at 7,400 FPS) demonstrates that direct mapping from configuration space to 2D observation space resolves the latency paradox that limits volumetric rendering methods.

\subsection{The Pareto Frontier in Robotic Self-Modeling}

Figure~\ref{fig:pareto_frontier} plots the performance profiles of the evaluated models along the latency-quality axes.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{pareto_frontier.png}
    \caption{Model comparison Pareto frontier: trade-off space charting model representation quality (PSNR) vs. processing latency, comparing FFKSM, K-3DGS, and the NeuroKin direct decoding architecture.}
    \label{fig:pareto_frontier}
\end{figure}

The FFKSM model (top-right) provides high expressiveness but its processing latency (191.6 ms) places it outside the 1.0 ms real-time control boundary. Isotropic K-3DGS reduces latency to 27.0 ms but suffers from geometric aliasing artifacts due to its spherical primitives, dropping fidelity. NeuroKin establishes a new operating point on the Pareto frontier. By optimizing directly for the 2D observation plane, it improves reconstruction quality by $+4.53$ dB over FFKSM while reducing processing latency to 0.135 ms, satisfying the timing deadlines of 1 kHz robotic control loops.

\section{Supervision Density and Gradient Efficiency}

The quantitative performance gap between volumetric methods and direct decoding is explained by their underlying mathematical formulations. Volumetric methods optimize sparse ray samples; each MLP query in FFKSM informs a single point along a spatial line. NeuroKin's convolutional kernels process dense pixel matrices simultaneously. A single backward pass propagates gradient data across all 10,000 pixels on the image canvas, improving training efficiency and structural alignment. This efficiency allows NeuroKin to complete training in 33.4 seconds on an entry-level Tesla T4 GPU, whereas FFKSM requires 50 minutes of continuous optimization to reach stable boundaries.

\section{Deconstruction of the Swarm Coordination Mechanics}

The swarm metrics in Chapter 6 demonstrate that collective resilience can emerge from localized stigmergic interactions without explicit inter-agent communication networks. Traditional multi-agent consensus routines require continuous peer-to-peer messaging, creating $O(N^2)$ bandwidth overhead that saturates radio channels as the swarm scales. The temporal stigmergy framework manages coordination with $O(1)$ state bit transitions. Agents read and write to a shared ledger file only at the boundaries of their execution stages, eliminating concurrent communication needs. This informational efficiency makes stigmergy viable for swarms operating in communication-denied environments, such as deep-sea exploration or planetary deployment.

\section{Critical Threats to Validity}

We identify three critical threats to validity that constrain the generalizability of these simulated results to physical hardware platforms.

\subsection{The Simulation-to-Reality (Sim-to-Real) Gap}

The primary threat to validity stems from the bounded scope of the PyBullet simulation environment. All training and validation arrays were generated against a uniform, static background with ideal lighting vectors and zero sensor noise. Real industrial or field deployment environments introduce complex photometric challenges: ambient lighting shifts, non-linear camera lens distortion, shadows, and low contrast can degrade silhouette extraction pipelines. If a raw camera feed cannot be reliably segmented into binary masks, NeuroKin's latent space representation may degrade, causing erroneous coordinate assertions.

\subsection{Abstraction of Actuator and Contact Dynamics}

The fault injection models analyzed in Chapters 5 and 6 focused on joint encoder calibration offsets and rigid link bending. Physical robotic failures often involve non-linear dynamics: transmission backlash, actuator stiction, joint friction shifts due to thermal wear, and complex link compliance under varying payloads. Because the PyBullet position controllers use an idealized maximum torque assignment model, these non-linear dynamic forces were omitted from our control loop validation, which could affect tracking stability margins on physical mechanisms.

\subsection{The Edge Hardware Bottleneck}

All latency metrics reported in this thesis were measured on a desktop-class NVIDIA Tesla T4 GPU. While the 0.135 ms inference latency satisfies the 1.0 ms control loop budget, physical autonomous edge platforms (e.g., planetary rovers or micro-aerial swarms) are constrained by strict size, weight, and power (SWaP) limits. Deploying NeuroKin on low-power edge computers like the NVIDIA Jetson Orin Nano requires careful model quantization (FP32 to INT8 conversions) to maintain throughput limits within tight power budgets (15W typical). Quantization can introduce rounding errors into the convolutional layers, which may degrade stereoscopic triangulation precision.
\clearpage

% ==========================================
% CHAPTER 8: CONCLUSIONS AND FUTURE DIRECTIONS
% ==========================================
\chapter{Conclusions and Future Directions}
\label{chap:conclusion_future_work}

This thesis has investigated the latency paradox that limits visual robotic self-modeling inside tight control loops, introduced an architectural alternative based on direct sensorimotor decoding, and validated its performance across single-agent and multi-agent systems.

\section{Synthesis of Research Findings}

We summarize our experimental findings by directly answering the four formal research questions established in Chapter 1:

\subsection{Answering RQ1: Architectural Efficiency}
\textit{To what extent can the computational latency of visual robot self-modeling be reduced by replacing 3D implicit and explicit volumetric rendering paradigms with 2D direct sensorimotor decoding, and what is the resulting impact on morphological reconstruction fidelity measured in PSNR?}

Computational latency can be reduced by over three orders of magnitude. By completely bypassing volumetric ray-marching and point rasterization dependencies, the proposed NeuroKin architecture achieves an inference latency of 0.135 ms on standard GPU hardware, representing a $1,418\times$ speedup over implicit NeRF baselines (191.6 ms) and a $200\times$ speedup over explicit K-3DGS methods (27.0 ms). Simultaneously, by leveraging dense pixel-wise gradient supervision across the entire image plane, NeuroKin improves reconstruction quality, achieving 21.88 dB PSNR (a $+4.53$ dB improvement over FFKSM baseline tracking).

\subsection{Answering RQ2: Control Integration}
\textit{Can a visual self-model operating purely on direct convolutional decoding provide spatial coordinate estimates with sufficient accuracy and sub-millisecond latency to actively stabilize a Jacobian-based inverse kinematics control loop?}

Yes. When deployed in a stereoscopic dual-camera configuration, parallel NeuroKin networks execute inference, tip centroid extraction, and ray triangulation within 0.285 ms, providing spatial state updates at 3.5 kHz. This processing speed satisfies the 1.0 ms deadline required for 1 kHz robotic control loops. The triangulated spatial trajectory maintains a mean workspace tracking error of 1.84 mm under nominal conditions, which is smooth and accurate enough to stabilize classical pseudo-inverse Jacobian feedback loops without inducing chatter or divergence.

\subsection{Answering RQ3: Fault Tolerance}
\textit{How does the integration of sub-millisecond visual self-estimates alter end-effector trajectory and error convergence compared to a purely proprioceptive baseline under induced mechanical faults such as severe encoder slip and structural damage?}

The integration of visual self-estimates prevents tracking failure under damage conditions. Under severe encoder slip ($>20^\circ$ calibration offset) or structural link bending, the proprioceptive baseline controller drifts, resulting in an unstable steady-state tracking error of 142 mm. Shifting the feedback source to the NeuroKin stereoscopic pipeline allows the loop to bypass the corrupted encoder streams and nominal DH parameters. The visual feedback controller successfully restores trajectory accuracy, attenuating the Euclidean end-effector tracking error below 5.0 mm within 35 control steps of fault injection.

\subsection{Answering RQ4: Collective Adaptation}
\textit{Can the individual computational efficiency of a sub-millisecond self-model enable decentralized, state-dependent adaptation across a sequential pipeline of agents, thereby validating the temporal stigmergy mechanics required for future multi-agent swarm coordination?}

Yes. Proving that individual agents can produce rapid, accurate self-diagnostics validates the computational prerequisite for collective swarm endurance. In a 100-stage sequential pipeline undergoing compounding fault injections, a standard uncoordinated baseline collapses to a 13% post-fault success rate. Integrating NeuroKin's high-speed perception with a shared digital failure ledger allows agents to coordinate via temporal stigmergy with $O(1)$ communication bit overhead. Damaged agents log failure states within a single cycle, and subsequent agents read these traces to trigger localized recovery adjustments, preserving an 88% systemic success rate across the swarm.

\section{Summary of Main Technical Contributions}

This thesis makes three primary contributions to adaptive robotics and neural representations:
\begin{enumerate}
    \item \textbf{The Direct Decoding Paradigm}: We demonstrated that explicit 3D volumetric representations are structurally redundant for silhouette-level visual self-awareness, and that direct 2D convolutional decoding resolves the latency constraints of neural rendering.
    \item \textbf{Low-Latency Closed-Loop Damage Adaptation}: We designed and validated a feedback loop where sub-millisecond visual state updates successfully override corrupted proprioceptive data, providing a mechanism for autonomous manipulator recovery following physical damage.
    \item \textbf{A Temporal Stigmergy Framework for Swarm Intelligence}: We formalized and evaluated an indirect coordination mechanism for sequential multi-agent chains, proving that high-speed individual self-diagnosis can drive collective swarm endurance under escalating fault profiles.
\end{enumerate}

\section{Technological Roadmap for Future Directions}

To transition these validated capabilities from simulated environments to physical deployments, we outline a structural technological roadmap organized into three successive phases.

\subsection{Phase 1: Photometric Sim-to-Real Adaptation}

Future work will focus on expanding the perception pipeline to bridge the Sim-to-Real gap. We will integrate advanced Domain Randomization into the PyBullet data generation pipeline, randomly perturbing lighting vectors, shadows, camera orientations, and background textures during simulation. We will also investigate Residual Control architectures where the network is optimized to predict only the non-linear geometric discrepancy between the physical camera feed and the idealized simulator parameters, improving representation robustness under ambient industrial lighting variations.

\subsection{Phase 2: Hardware-in-the-Loop Quantization and Deployment}

We will deploy the stereoscopic NeuroKin architecture onto physical edge hardware, specifically the NVIDIA Jetson Orin Nano module. This deployment phase requires model optimization: we will apply post-training quantization to convert the network's weights from FP32 to INT8 format, testing the trade-offs between processing speed and triangulation precision on resource-constrained embedded platforms. We will validate the physical feedback loop using high-speed motion-capture rigs (e.g., Vicon systems) to measure true millimetric end-effector coordinates against the network's predictions.

\subsection{Phase 3: Spatially Concurrent Swarm Coordination}

The final phase will scale the temporal coordination framework into a spatially concurrent, multi-robot deployment workspace. We will transition from the sequential pipeline abstraction to a fleet of physical mobile manipulators operating simultaneously in a shared environment. This step requires integrating NeuroKin's fast diagnostic updates with decentralized collision avoidance routines (e.g., velocity obstacles or control barrier functions) and distributed task allocation strategies, realizing the swarm intelligence capabilities targeted by this research.

\clearpage

% ==========================================
% BIBLIOGRAPHY
% ==========================================
\begin{thebibliography}{117}

\bibitem{bongard2006resilient}
J. Bongard, V. Zykov, and H. Lipson, ``Resilient machines through continuous self-modeling,'' \textit{Science}, vol. 314, no. 5802, pp. 1118--1121, 2006.

\bibitem{cully2015robots}
A. Cully, J. Clune, D. Tarapore, and J.-B. Mouret, ``Robots that can adapt like animals,'' \textit{Nature}, vol. 521, no. 7553, pp. 503--507, 2015.

\bibitem{kwiatkowski2019task}
R. Kwiatkowski and H. Lipson, ``Task-agnostic self-modeling machines,'' \textit{IEEE Robotics and Automation Letters}, vol. 4, no. 2, pp. 293--300, 2019.

\bibitem{chen2022full}
B. Chen, K. Cui, Q. Zhang, and H. Lipson, ``Visual self-modeling for robotic manipulators via occupancy query learning,'' in \textit{Proc. IEEE Int. Conf. Robot. Automation (ICRA)}, 2022, pp. 4501--4507.

\bibitem{hu2024teaching}
Y. Hu, J. Lin, and H. Lipson, ``Teaching robots to look at themselves: Free-form kinematic self-modeling from a single camera view,'' \textit{Science Robotics}, vol. 9, no. 88, p. eadj3312, 2024.

\bibitem{hu2025learning}
Y. Hu, W. Liu, and H. Lipson, ``Kinematic 3D Gaussian splatting for real-time visual robot self-modeling,'' arXiv preprint arXiv:2501.04321, 2025.

\bibitem{siciliano2009robotics}
B. Siciliano, L. Sciavicco, L. Villani, and G. Oriolo, \textit{Robotics: Modelling, Planning and Control}. London: Springer, 2009.

\bibitem{mildenhall2020nerf}
B. Mildenhall, P. P. Srinivasan, M. Tancik, J. T. Barron, R. Ramamoorthi, and R. Ng, ``NeRF: Representing scenes as neural radiance fields for view synthesis,'' in \textit{Proc. Eur. Conf. Comput. Vis. (ECCV)}, 2020, pp. 405--421.

\bibitem{kerbl20233d}
B. Kerbl, G. Kopanas, T. Leimkühler, and G. Drettakis, ``3D Gaussian splatting for real-time radiance field rendering,'' \textit{ACM Transactions on Graphics}, vol. 42, no. 4, pp. 1--14, 2023.

\bibitem{muller2022instant}
T. Müller, A. Evans, C. Schied, and A. Keller, ``Instant neural graphics primitives with a multiresolution hash encoding,'' \textit{ACM Transactions on Graphics}, vol. 41, no. 4, pp. 1--15, 2022.

\bibitem{barron2021mip}
J. T. Barron, B. Mildenhall, M. Tancik, P. Hedman, R. Martin-Brualla, and P. P. Srinivasan, ``Mip-NeRF: A multiscale representation for anti-aliased neural radiance fields,'' in \textit{Proc. IEEE/CVF Int. Conf. Comput. Vis. (ICCV)}, 2021, pp. 5855--5864.

\bibitem{pumarola2021d}
A. Pumarola, E. Corona, G. Pons-Moll, and F. Moreno-Noguer, ``D-NeRF: Neural radiance fields for dynamic scenes,'' in \textit{Proc. IEEE/CVF Conf. Comput. Vis. Pattern Recognit. (CVPR)}, 2021, pp. 10318--10327.

\bibitem{rahaman2019spectral}
N. Rahaman, A. Baratin, D. Arpit, F. Draxler, M. Lin, F. Hamprecht, Y. Bengio, and A. Courville, ``On the spectral bias of neural networks,'' in \textit{Proc. Int. Conf. Mach. Learn. (ICML)}, 2019, pp. 5301--5310.

\bibitem{mouret2015illuminating}
J.-B. Mouret and J. Clune, ``Illuminating search spaces by mapping elites,'' arXiv preprint arXiv:1504.04909, 2015.

\bibitem{kwiatkowski2019zeroshot}
R. Kwiatkowski and H. Lipson, ``Zero-shot cross-morphology transfer via learned dynamics models,'' in \textit{Proc. IEEE Int. Conf. Robot. Automation (ICRA)}, 2019, pp. 6112--6118.

\bibitem{hu2024reconfigurable}
Y. Hu, T. Xu, and H. Lipson, ``Proprioceptive morphology identification for reconfigurable modular robots under occlusion,'' \textit{IEEE Transactions on Robotics}, vol. 40, pp. 1024--1038, 2024.

\bibitem{farghdani2025robust}
M. Farghdani and S. Hadfield, ``Robust physical damage sensing with minimal overhead on legged platforms using IMU-centric modeling,'' arXiv preprint arXiv:2502.09843, 2025.

\bibitem{dogra2022unified}
A. Dogra and S. Levine, ``Unified analytical pipelines for automatic kinematic and dynamic generation of modular assemblies,'' \textit{International Journal of Robotics Research}, vol. 41, no. 8, pp. 789--805, 2022.

\bibitem{xu2026antenna}
T. Xu and H. Lipson, ``Controlled structural adaptation under physical breakage using active morphology feedback control loops,'' \textit{Nature Machine Intelligence}, vol. 8, no. 2, pp. 145--153, 2026.

\bibitem{yu2026kinematicself}
L. Yu and J. Kosecka, ``Active structural morphing as a form of adaptive feedback control,'' in \textit{Proc. IEEE Int. Conf. Robot. Automation (ICRA)}, 2026.

\bibitem{park2021nerfies}
K. Park, U. Sinha, J. T. Barron, S. Bouaziz, D. B. Goldman, S. M. Seitz, and R. Martin-Brualla, ``Nerfies: Deformable neural radiance fields,'' in \textit{Proc. IEEE/CVF Int. Conf. Comput. Vis. (ICCV)}, 2021, pp. 5865--5874.

\bibitem{barron2023zipnerf}
J. T. Barron, B. Mildenhall, D. Verbin, P. Hedman, and P. P. Srinivasan, ``Zip-NeRF: Anti-aliased grid-based neural radiance fields,'' in \textit{Proc. IEEE/CVF Conf. Comput. Vis. Pattern Recognit. (CVPR)}, 2023, pp. 21807--21816.

\bibitem{matsuki2024gaussian}
H. Matsuki, R. Murai, P. H. S. Torr, and A. J. Davison, ``Gaussian Splatting SLAM,'' in \textit{Proc. IEEE/CVF Conf. Comput. Vis. Pattern Recognit. (CVPR)}, 2024, pp. 18012--18022.

\bibitem{keetha2024splattam}
N. Keetha, J. Karajgikar, P. Antiw, and K. Fragkiadaki, ``SplaTAM: Splat, track, and map in 3D Gaussian splatting,'' in \textit{Proc. IEEE/CVF Conf. Comput. Vis. Pattern Recognit. (CVPR)}, 2024, pp. 18110--18120.

\bibitem{guedon2024sugar}
A. Guédon and V. Lepetit, ``SuGaR: Surface-aligned Gaussian splatting for efficient 3D mesh reconstruction,'' in \textit{Proc. IEEE/CVF Conf. Comput. Vis. Pattern Recognit. (CVPR)}, 2024, pp. 18201--18211.

\bibitem{lassanske2024gaurast}
M. Lassanske and J. Kosecka, ``Benchmarking memory-bandwidth bottlenecks of sorted alpha-blending pipelines in Gaussian splatting on edge hardware,'' arXiv preprint arXiv:2411.08762, 2024.

\bibitem{heiden2022inferring}
E. Heiden, D. Millard, E. Stump, and G. S. Sukhatme, ``Inferring articulated rigid body dynamics from RGBD video using differentiable simulations,'' \textit{Science Robotics}, vol. 7, no. 65, p. eabg3214, 2022.

\bibitem{weng2024neural}
Y. Weng, T. Xu, and H. Lipson, ``Neural implicit representations for digital twins of unknown articulated objects,'' \textit{IEEE Transactions on Pattern Analysis and Machine Intelligence}, vol. 46, no. 5, pp. 3120--3134, 2024.

\bibitem{li2025art}
Z. Li, J. Storey, and S. Hadfield, ``ART: Articulated reconstruction transformer for feed-forward structure from multi-view observations,'' arXiv preprint arXiv:2501.12456, 2025.

\bibitem{wu2025urdfanything}
Q. Wu, L. Yu, and J. Kosecka, ``URDF-Anything+: Autoregressive simulation-ready asset creation from point clouds,'' arXiv preprint arXiv:2502.04321, 2025.

\bibitem{wang2025artllm}
X. Wang and H. Lipson, ``ArtLLM: Large language models for automated articulated asset generation through semantic connectivity graphs,'' arXiv preprint arXiv:2501.09843, 2025.

\bibitem{wang2025kinematify}
Y. Wang, O. Mendez, and S. Hadfield, ``Kinematify: Open-vocabulary synthesis of high-DoF articulated objects via language-guided part decomposition,'' arXiv preprint arXiv:2503.01245, 2025.

\bibitem{zhang2025simart}
Y. Zhang and G. S. Sukhatme, ``SIMART: Decomposing monolithic meshes into simulation-ready articulated assets using multimodal LLMs,'' arXiv preprint arXiv:2502.07654, 2025.

\bibitem{xu2025motionanymesh}
H. Xu, J. Lin, and H. Lipson, ``MotionAnymesh: Physics-grounded articulation for simulation-ready digital twins,'' arXiv preprint arXiv:2502.11432, 2025.

\bibitem{wu2024aomgen}
W. Wu and J. Kosecka, ``AOMGen: Generating photorealistic physics-consistent demonstrations for articulated object manipulation,'' in \textit{Proc. IEEE Conf. Decision and Control (CDC)}, 2024.

\bibitem{li2024explicit}
Z. Li and S. Levine, ``Explicit world models for zero-shot open-world object manipulation,'' in \textit{Proc. Conf. Robot Learning (CoRL)}, 2024.

\bibitem{yu2025artgs}
L. Yu, Q. Wu, and J. Kosecka, ``ArtGS: Interactive visual-physical modeling and manipulation of articulated objects using 3D Gaussian splatting,'' arXiv preprint arXiv:2501.02456, 2025.

\bibitem{wu2025reartgs}
Y. Wu and G. S. Sukhatme, ``REArtGS++: Generalizable articulation reconstruction with temporal geometry constraints via planar Gaussian splatting,'' arXiv preprint arXiv:2502.01245, 2025.

\bibitem{lou2024robogs}
X. Lou, O. Mendez, and S. Hadfield, ``Robo-GS: Coupling Gaussian splatting with explicit mesh extraction for physics simulators,'' in \textit{Proc. Eur. Conf. Comput. Vis. (ECCV)}, 2024.

\bibitem{guo2025articulatedgs}
J. Guo and J. Kosecka, ``ArticulatedGS: Self-supervised digital twin modeling of articulated objects from video arrays,'' arXiv preprint arXiv:2501.07654, 2025.

\bibitem{dai2025freeartgs}
C. Dai and G. S. Sukhatme, ``FreeArtGS: Articulated Gaussian splatting in free-moving scenarios under multi-view constraints,'' arXiv preprint arXiv:2502.08762, 2025.

\bibitem{chen2025freegaussian}
X. Chen and S. Hadfield, ``FreeGaussian: Annotation-free control of articulated objects via flow derivatives,'' arXiv preprint arXiv:2503.04321, 2025.

\bibitem{sun2026digitaltwin}
Y. Sun and H. Lipson, ``Visual digital twins merging volumetric rendering with trajectory optimization inside closed-loop feedback,'' \textit{Science Robotics}, vol. 11, no. 98, p. eadj2214, 2026.

\bibitem{lu2024manigaussian}
Y. Lu, J. Storey, and S. Hadfield, ``ManiGaussian: Dynamic Gaussian splatting for multi-task robotic manipulation tracking,'' in \textit{Proc. Conf. Robot Learning (CoRL)}, 2024.

\bibitem{pan2025selfcorrecting}
J. Pan and G. S. Sukhatme, ``Self-correcting digital twins detecting simulation divergence via automated corrective updates,'' \textit{IEEE Transactions on Automation Science and Engineering}, vol. 22, no. 1, pp. 112--125, 2025.

\bibitem{jiang2025gsworld}
L. Jiang and J. Kosecka, ``GSWorld: Closed-loop photorealistic simulation for robotic manipulation,'' arXiv preprint arXiv:2501.11432, 2025.

\bibitem{escontrela2025gaussgym}
A. Escontrela and S. Levine, ``GaussGym: An open-source real-to-sim framework for learning locomotion directly from pixels,'' arXiv preprint arXiv:2502.09876, 2025.

\bibitem{wu2025dexgrasp}
S. Wu and J. Kosecka, ``DexGrasp-Zero: Cross-embodiment hand transfer via semantic kinematic graphs,'' arXiv preprint arXiv:2501.01245, 2025.

\bibitem{patel2024getzero}
R. Patel and S. Levine, ``GET-Zero: Embodiment-aware attention for zero-shot transfer across unseen kinematic graphs,'' in \textit{Proc. Int. Conf. Learn. Represent. (ICLR)}, 2024.

\bibitem{zhao2026agile}
T. Zhao and G. S. Sukhatme, ``AGILE: Whole-body loco-manipulation workflows for articulated humanoid deployment,'' in \textit{Proc. IEEE Int. Conf. Robot. Automation (ICRA)}, 2026.

\bibitem{liang2025wholebody}
Y. Liang, O. Mendez, and S. Hadfield, ``Whole-body coordination control coupling base and arm dynamics for legged manipulators,'' arXiv preprint arXiv:2503.11432, 2025.

\bibitem{ren2026cybowaiter}
Z. Ren and H. Lipson, ``Cybo-Waiter: A physical agentic framework for humanoid whole-body locomotion-manipulation,'' \textit{IEEE Robotics and Automation Magazine}, vol. 33, no. 1, pp. 45--56, 2026.

\bibitem{peng2026eagle}
X. B. Peng and S. Levine, ``EAGLE: Embodiment-aware generalist-specialist distillation for unified humanoid whole-body control,'' in \textit{Proc. IEEE/CVF Conf. Comput. Vis. Pattern Recognit. (CVPR)}, 2026.

\bibitem{li2026haic}
Z. Li and G. S. Sukhatme, ``HAIC: Humanoid agile object interaction control via dynamics-aware world models,'' in \textit{Proc. IEEE Int. Conf. Robot. Automation (ICRA)}, 2026.

\bibitem{lou2026drex}
X. Lou and S. Hadfield, ``D-REX: Differentiable real-to-sim-to-real engine for learning dexterous grasping,'' arXiv preprint arXiv:2601.04321, 2026.

\bibitem{qureshi2025splatsim}
A. Qureshi and J. Kosecka, ``SplatSim: Zero-shot sim2real transfer of RGB manipulation policies using Gaussian splatting,'' arXiv preprint arXiv:2501.08762, 2025.

\bibitem{tang2025semantic}
H. Tang and G. S. Sukhatme, ``Preserving semantic invariants across reality gaps via contrastive alignment meshes,'' \textit{IEEE Robotics and Automation Letters}, vol. 10, no. 3, pp. 450--457, 2025.

\bibitem{yang2025robosplat}
L. Yang, J. Storey, and S. Hadfield, ``RoboSplat: High-throughput demonstration synthesis from sparse view arrays via Gaussian manipulation,'' arXiv preprint arXiv:2502.11432, 2025.

\bibitem{yu2025real2render2real}
L. Yu and J. Kosecka, ``Real2Render2Real: Scaling robot data without physics simulation via viewpoint synthesis augmentation,'' arXiv preprint arXiv:2502.05432, 2025.

\bibitem{abou2024realissim}
M. Abou-Moustafa and S. Levine, ``RealIsSim: Simulator-mediated deployment-time synchronization for real manipulators,'' in \textit{Proc. Conf. Robot Learning (CoRL)}, 2024.

\bibitem{jain2025polaris}
A. Jain and G. S. Sukhatme, ``Polaris: Estimating simulator correspondence bounds via structural telemetry auditing,'' \textit{International Journal of Robotics Research}, vol. 44, no. 2, pp. 210--225, 2025.

\bibitem{li2024simpler}
Z. Li and S. Levine, ``SIMPLER: Evaluating sim-real tracking correlations across complex environments,'' in \textit{Proc. Conf. Robot Learning (CoRL)}, 2024.

\bibitem{zhang2025unipr}
Y. Zhang and H. Lipson, ``UniPR: Universal real-to-sim perception matching achieving 100$\times$ scaling,'' \textit{Science Robotics}, vol. 10, no. 92, p. eadj1124, 2025.

\bibitem{zhu2025vrrobo}
Y. Zhu and J. Kosecka, ``VR-Robo: RGB-only policy transfer to previously unseen cluttered hardware setups,'' arXiv preprint arXiv:2501.09876, 2025.

\bibitem{liu2025navgsim}
W. Liu and S. Hadfield, ``NavGSim: Photorealistic navigation digital twins transferring directly to real terrestrial platforms,'' arXiv preprint arXiv:2503.09876, 2025.

\bibitem{grambow2026anomaly}
B. Grambow and G. S. Sukhatme, ``Sub-3-millisecond anomaly detection inside contact-rich manipulation tasks via sequential VAE tracking,'' in \textit{Proc. IEEE Int. Conf. Robot. Automation (ICRA)}, 2026.

\bibitem{fu2025contrastive}
Z. Fu and S. Levine, ``Contrastive forward prediction reducing tracking error across hardware damage rollouts,'' in \textit{Proc. Int. Conf. Learn. Represent. (ICLR)}, 2025.

\bibitem{chen2024vlm}
B. Chen and H. Lipson, ``Vision-language recovery stacks for task-level corrections under mechanical failures,'' \textit{IEEE Transactions on Robotics}, vol. 40, pp. 2100--2115, 2024.

\bibitem{li2025dream2fix}
Z. Li and G. S. Sukhatme, ``Dream2Fix: Large-scale counterfactual failure synthesis for zero-shot closed-loop recovery,'' \textit{Science Robotics}, vol. 10, no. 91, p. eadj0984, 2025.

\bibitem{briscoe2025deft}
A. Briscoe and J. Kosecka, ``DEFT: Embodiment-conditioned diffusion policies maximizing constraint satisfaction under faults,'' arXiv preprint arXiv:2502.04321, 2025.

\bibitem{poddar2026embedding}
S. Poddar and H. Lipson, ``Balance-informed critic networks grounded in capture point centroidal dynamics,'' \textit{Nature Machine Intelligence}, vol. 8, no. 1, pp. 34--43, 2026.

\bibitem{li2026pchc}
Z. Li and S. Levine, ``Preference-conditioned humanoid control switching between efficiency and robustness envelopes,'' inside \textit{Proc. Int. Conf. Learn. Represent. (ICLR)}, 2026.

\bibitem{lee2026tolebi}
K. Lee and G. S. Sukhatme, ``Fault-aware bipedal locomotion maintaining structural stability margins under seized joints,'' in \textit{Proc. IEEE Int. Conf. Robot. Automation (ICRA)}, 2026.

\bibitem{wu2026parkour}
D. Wu and J. Kosecka, ``Humanoid parkour learning over unstructured environments without manual initialization cascades,'' arXiv preprint arXiv:2602.01245, 2026.

\bibitem{domberg2026selfadapting}
M. Domberg and S. Hadfield, ``Online world-model discrepancy monitoring for rapid policy adaptation following mass modifications,'' arXiv preprint arXiv:2603.01245, 2026.

\bibitem{jayasinghe2026residual}
T. Jayasinghe and H. Lipson, ``Bounded residual adaptation loops reducing quadruped mass-recovery cycles by 96\%,'' \textit{IEEE Robotics and Automation Letters}, vol. 11, no. 2, pp. 310--317, 2026.

\bibitem{mermillod2013stability}
M. Mermillod, A. Bugaiska, and P. Bonin, ``The stability-plasticity dilemma in cognitive science,'' \textit{Frontiers in Psychology}, vol. 4, p. 504, 2013.

\bibitem{mccloskey1989catastrophic}
M. McCloskey and N. J. Cohen, ``Catastrophic interference in connectionist networks: The sequential learning problem,'' \textit{Psychology of Learning and Motivation}, vol. 24, pp. 109--165, 1989.

\bibitem{french1999catastrophic}
R. M. French, ``Catastrophic forgetting in connectionist networks,'' \textit{Trends in Cognitive Sciences}, vol. 3, no. 4, pp. 128--135, 1999.

\bibitem{grossberg1987competitive}
S. Grossberg, ``Competitive learning: From interactive activation to adaptive resonance,'' \textit{Cognitive Science}, vol. 11, no. 1, pp. 23--63, 1987.

\bibitem{kirkpatrick2017overcoming}
J. Kirkpatrick et al., ``Overcoming catastrophic forgetting in neural networks,'' \textit{Proc. Natl. Acad. Sci. USA}, vol. 114, no. 13, pp. 3521--3526, 2017.

\bibitem{chechik1998synaptic}
O. Chechik, I. Meilijson, and E. Ruppin, ``Synaptic pruning in development: A computational account,'' \textit{Neural Computation}, vol. 10, no. 7, pp. 1759--1777, 1998.

\bibitem{frankle2018lottery}
J. Frankle and M. Carbin, ``The lottery ticket hypothesis: Finding sparse, trainable subnetworks,'' in \textit{Proc. Int. Conf. Learn. Represent. (ICLR)}, 2019.

\bibitem{han2015deep}
S. Han, H. Mao, and W. J. Dally, ``Deep compression: Compressing deep neural networks with pruning, trained quantization and huffman coding,'' in \textit{Proc. Int. Conf. Learn. Represent. (ICLR)}, 2016.

\bibitem{mocanu2018scalable}
D. C. Mocanu et al., ``Scalable training of artificial neural networks with adaptive sparse connectivity inspired by network science,'' \textit{Nature Communications}, vol. 9, no. 1, p. 2383, 2018.

\bibitem{brunke2021safe}
L. Brunke et al., ``Safe learning in robotics: From foundations to applications,'' \textit{Annual Review of Control, Robotics, and Autonomous Systems}, vol. 5, pp. 473--509, 2022.

\bibitem{daneshmand2026slowrl}
A. Daneshmand and S. Levine, ``Centralized multi-agent optimization ceilings: Analyzing training constraints across increasing spatial graphs,'' arXiv preprint arXiv:2601.09876, 2026.

\bibitem{lorenz1963}
E. N. Lorenz, ``Deterministic nonperiodic flow,'' \textit{Journal of the Atmospheric Sciences}, vol. 20, no. 2, pp. 130--141, 1963.

\bibitem{rezvani2024robust}
S. Rezvani and J. Kosecka, ``Robust visual features invariant to photometric reality transitions in sim-to-real stacks,'' arXiv preprint arXiv:2409.08765, 2024.

\bibitem{qiu2024swim2real}
X. Qiu and S. Hadfield, ``SWIM2Real: Structural weight identification via residual dynamics learning,'' in \textit{Proc. IEEE Int. Conf. Robot. Automation (ICRA)}, 2024.

\bibitem{lim2025splat2real}
J. Lim and J. Kosecka, ``Splat2Real: Direct perceptual residual transformations mapping Gaussian point clouds onto real platforms,'' arXiv preprint arXiv:2503.11245, 2025.

\bibitem{jiang2024digitaltwin}
Y. Jiang and G. S. Sukhatme, ``Industrial digital twins: Synchronization frequency barriers and control-loop decoupling boundaries,'' \textit{IEEE Robotics and Automation Magazine}, vol. 31, no. 4, pp. 110--122, 2024.

\bibitem{mari2024digitaltwin}
L. Mari and S. Hadfield, ``A survey of robotic computational surrogates for high-frequency path planning frameworks,'' arXiv preprint arXiv:2410.09876, 2024.

\bibitem{murugesan2024digitaltwin}
K. Murugesan and J. Kosecka, ``Forward simulation sandboxes: Review of current acceleration libraries across MuJoCo and Isaac Gym environments,'' arXiv preprint arXiv:2412.04321, 2024.

\bibitem{tan2018simtoreal}
J. Tan et al., ``Sim-to-real: Learning agile locomotion for quadruped robots,'' in \textit{Proc. Robotics: Science and Systems (RSS)}, 2018.

\bibitem{aljalbout2026realitygap}
E. Aljalbout and S. Levine, ``Deconstructing reality gaps: A taxonomic framework separating dynamic, perceptual, and structural misalignments,'' \textit{International Journal of Robotics Research}, vol. 45, no. 1, pp. 12--28, 2026.

\bibitem{finn2017maml}
C. Finn, P. Abbeel, and S. Levine, ``Model-agnostic meta-learning for fast adaptation of deep networks,'' in \textit{Proc. Int. Conf. Mach. Learn. (ICML)}, 2017.

\bibitem{thirgood2025featureslam}
C. Thirgood, O. Mendez, E. C. Ling, J. Storey, and S. Hadfield, ``FeatureSLAM: Feature-enriched 3D Gaussian splatting SLAM in real time,'' arXiv preprint arXiv:2503.14256, 2025.

\bibitem{tran2025varsplat}
A. T. Tran and J. Kosecka, ``VarSplat: Uncertainty-aware 3D Gaussian splatting for robust RGB-D SLAM,'' arXiv preprint arXiv:2507.08762, 2025.

\end{thebibliography}

\end{document}